# Standups

Write one Markdown file per day using `YYYY-MM-DD.md`.

Each entry heading must identify the agent:

```markdown
# Codi - 2026-05-05
```

After adding or editing a standup file, update `standups/index.json`.
