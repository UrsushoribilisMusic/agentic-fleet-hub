# Standup: 2026-06-25

## Activity Summary
- Agents called: Clau (Claude Code)
- Sessions: 1
- Tasks completed: eval infrastructure rebuilt; eval run in progress

## What was done today

### EVAL-007 v2 — apertus-v2 unblocked and relaunched
**Root cause of all v2 failures:** The apertus-v2 GGUF (`sha256-1fef6862…`) had a complex TypeScript/tool-calling Jinja2 template embedded in its metadata. Ollama 0.30.x passes this to llama-server, which crashes on every request with "Automatic parser generation failed".

**Fix:** In-place byte patch at offset 7,860,109 in the GGUF — replaced the broken 14,601-byte template with the working template from apertus-flotilla (same byte length, no file copy, no manifest change needed).

**Eval set rebuilt:** The original 200-question eval set lived at `~/projects/fx/out/datasets/eval.jsonl` (the `fx/` project directory is gone). Reconstructed 200 questions from the v1 results file (`results_eval007.jsonl`) — 178 full, 22 truncated at 300 chars (minor known limitation). Written to `eval/eval007_full.jsonl`.

**Running now:** `run_eval007_v2_armb.py` — 200 questions, Arm A answers reused from v1, Arm B (apertus-v2) re-generated fresh. 600 s timeout per question. Grader (`grade_eval007.py`) chains automatically when done. Output: `eval/results_eval007_v2_full.jsonl`.

### Documentation
- `eval/README.md` written: experiment design, file map, v1 results, model notes, restart commands, GGUF fix documented.

### Agentegra website
- `agentegra.com` homepage replaced with new `agentegra3.html` (sovereign AI pitch, "compile your knowledge into a cited wiki + baked model" framing).

## Notes
- `~/projects/fx/` directory no longer exists — 22 of 200 eval questions have truncated prompts as a result. Compare v2 vs v1 results with this caveat in mind.
- apertus architecture (`general.architecture: apertus`, xielu activations) must go through Ollama's Go template layer — direct llama-server calls produce garbage output.
- Run started ~11:00 CET, expected to finish overnight (200 q × ~30–60 s avg = 2–3 h).
