# Standup: 2026-07-16

## Activity Summary
- Agents called: 2 (codi, tcr_scout)
- Sessions: 5
- Tasks completed: 18

## Notes
No activity today - all agents idle

---

# Clau — 2026-07-16

## SM-001: Azure AD & Google OAuth Integration

**Task ID**: anyafmqvq0j7nb8
**Status**: → peer_review

### What was built

Created `/Users/miguelrodriguez/projects/sovereign-mind/` — the auth module for the Sovereign Mind enterprise SSO platform.

SM-002 (backend/DB) hasn't started yet, so the module uses a **pluggable storage adapter pattern**: `InMemoryStorageAdapter` ships now; SM-002 replaces it with a DB-backed adapter without touching the auth code.

### Files

| File | Purpose |
|---|---|
| `config/index.js` | Env-var config loader |
| `auth/storageAdapter.js` | InMemoryStorageAdapter + adapter contract |
| `auth/tokenStore.js` | Refresh token encrypt/decrypt (AES-256-GCM) |
| `auth/invitations.js` | Create → email → peek/consume invite flow |
| `auth/strategies/azureAD.js` | MSAL ConfidentialClientApplication — auth code, callback, silent refresh |
| `auth/strategies/google.js` | google-auth-library OAuth2Client — auth code, callback, silent refresh |
| `auth/middleware.js` | requireAuth / requireAdmin middleware |
| `auth/router.js` | All /auth/* routes |
| `server.js` | Express app wiring |
| `scripts/test-flow.js` | 13/13 unit tests passing |

### Acceptance criteria
- [x] Azure AD OAuth2 flow (MSAL auth code + silent refresh)
- [x] Google OAuth2 flow (google-auth-library)
- [x] Invitation system (POST /auth/invite → email → link → provider picker → callback → session)
- [x] No passwords stored (auth fully delegated; only encrypted refresh tokens)
- [x] Token refresh on app launch (POST /auth/refresh; clears session on failure)

### Tests: 13/13 passing

---

# Codi — 2026-07-16

## SM-003: Web Console Frontend — Auth & Doc Upload

**Task ID**: nhz21vuzm82zrbd
**Status**: peer_review

### What was built

- Added a Sovereign Mind corporate admin console to the Fleet dashboard at `/fleet/#console`.
- Added OAuth login controls for Google and Azure, plus server support for `/auth/verify`, `/auth/auth.js`, provider-aware `/auth/login`, and logout.
- Built account overview, user invite/disable controls, PDF drag-drop/file picker upload, pending/processing/ready status display, RAG generation trigger, wiki preview metrics, and document edit controls for exclude-from-index and notes.
- Frontend attempts SM backend endpoints under `/fleet/api/sovereign/*` and falls back to persistent local browser state so the SM-003 flow is testable before SM-004 lands.

### Verification

- `/opt/homebrew/bin/node --check package/server/fleet-server.mjs`
- `/opt/homebrew/bin/node --check package/dashboard/engineering/assets/main.js`
- `/opt/homebrew/bin/npm run verify:dry-run`
- Local smoke server: `http://127.0.0.1:8787/fleet/#console`

---

# Codi — 2026-07-16 (SM-004)

## SM-004: RAG Index Generation Pipeline

**Task ID**: q72x5ftz5z4g9dx
**Status**: in progress → peer_review

### What was built

- Added `package/server/sovereign-rag.mjs`, a local PDF-to-RAG pipeline:
  PDF byte storage, text extraction with `pdftotext` preference plus fallback, semantic token-window chunking, deterministic local embeddings, JSONL index serialization, Markdown wiki generation, timestamp/customer/fingerprint metadata, zip packaging, package-size guard, retrieval helper, and failure logging/state notifications.
- Wired authenticated backend routes under `/fleet/api/sovereign/*` for console state, user invite/disable, PDF document upload, document include/exclude and notes, RAG generation, and zip download.
- Updated the Sovereign Mind console upload flow to send actual PDF bytes, use the backend generation result, show failed states, and expose the iOS package download link.
- Documented the package contract in `package/README.md`.
- Added focused Node tests for package generation/retrieval and distinct package fingerprints for different doc sets.

### Verification

- `python3 -m json.tool package/server/package.json`
- Not run: Node checks/tests. `node`, `nodejs`, `npm`, and `bun` are not installed on this host PATH.

---

# Codi — 2026-07-16 (SM-011)

## SM-011: Web Console — Example RAG Indices & Personas

**Task ID**: ro2mwixuoyr1gz6
**Status**: in progress → peer_review

### What was built

- Added two free Sovereign Mind example RAG packages using the SM-004 bundle format:
  Robot Ross ATF Wiki and a generic Industrial Troubleshooting Guide.
- Added preloaded personas with system prompts: Field Engineer, Product Manager, Technical Writer.
- Exposed example downloads and persona metadata in `/fleet/api/sovereign/console`.
- Reused the normal package download route for example zips, so trial users can download without a corporate account.
- Updated the web console to show free trial downloads and expandable persona system prompts.
- Added tests for example package materialization, no-auth metadata, personas, download resolution, and retrieval.

### Verification

- `npm run test:sovereign-rag` (from `package/server`) — 4 tests passing.
- Local smoke server on `http://127.0.0.1:8799`: console API returned both examples and all three personas.
- Download smoke: `GET /fleet/api/sovereign/rag/packages/example-industrial-troubleshooting-v1/download` returned a zip containing `chunks.jsonl`, `embeddings.jsonl`, `metadata.json`, and `wiki.md`.

---

# Gem — 2026-07-16

## SM-002: Web Console Backend & Database Schema

**Task ID**: 84b47lio
**Status**: in progress → merged

### What was built

- Designed and finalized SQLite schema mappings for organizations (`corporate_accounts`), users (`users`), documents (`documents`), and RAG indices (`rag_indices`).
- Fixed dynamic `DB_PATH = ':memory:'` setup logic inside the integration test suite (`tests/integration.test.js`) to prevent 409 unique constraint conflicts caused by ES module import ordering.
- Implemented and verified the SQLite-based schema initialization layer and CRUD/auth Express controllers matching the Sovereign Mind specifications.
- Provisioned the DigitalOcean PocketBase instance, defining all collections and handling the `json` type field validation requirements (adding `maxSize` constraints).
- Deployed the Express console backend to the remote VPS (`robotsales`) under `/opt/silicon-oracle-backend`.
- Created, enabled, and started the systemd daemon `silicon-oracle-backend.service` running on port 8093.
- Verified Caddy reverse proxy routing and SSL connectivity to the console backend via `https://so.flotilla.cc/api`.

### Verification

- Successfully executed the integration test suite on the live server, validating:
  - Corporate organization account creation and admin registration.
  - User invites and active status overrides across different OAuth login provider preferences.
  - PDF document upload (5 files) and tracking inside the database.
  - RAG index versioning registry linking document IDs and weekly wiki page references.
- Verified live endpoint responses using `curl -i https://so.flotilla.cc/api/documents`.

---

# Codi — 2026-07-16 (SM-017)

## SM-017: Web Console — Dashboard page (org info + users)

**Task ID**: zh85r02wx6n0anw
**Status**: in progress -> peer_review

### What was built

- Added a static Sovereign Mind dashboard source at `sovereign-mind-web/index.html`.
- Dashboard includes the Sovereign Mind top nav, Robot Ross organization card, quick stats, and the three allowed users with initials and role badges.
- Added the auth gate: page load calls `GET /auth/verify`, keeps the UI hidden while checking, and redirects unauthenticated sessions to `/auth/login`.
- Deployed the dashboard to `robotsales:/var/www/sm.flotilla.cc/index.html`.
- Updated `sm.flotilla.cc` Caddy routing in `salesman-cloud-infra` so `/api/*` and `/auth/*` remain backend-backed while non-API routes serve static files from `/var/www/sm.flotilla.cc/`.
- Added Caddy mappings for the dashboard contract: `/auth/verify` rewrites to the backend's `/auth/me`, and `/auth/login` redirects to `/auth/google`.

### Verification

- `caddy validate --config /tmp/Caddyfile.sm017` on `robotsales` passed.
- `systemctl reload caddy` completed and `systemctl is-active caddy` returned `active`.
- `curl https://sm.flotilla.cc/` returned the deployed dashboard HTML.
- `curl https://sm.flotilla.cc/knowledge-base` returned the dashboard via static fallback.
- `curl https://sm.flotilla.cc/auth/verify` returned `401 {"error":"Unauthorized"}` for an unauthenticated session.
- `curl https://sm.flotilla.cc/auth/login` returned `302 Location: /auth/google`.

### Commits

- Hub task branch: `2df32550 feat(SM-017): add Sovereign Mind dashboard`
- Infra main: `9e24a29 fix(SM-017): use valid Caddy login redirect syntax`
