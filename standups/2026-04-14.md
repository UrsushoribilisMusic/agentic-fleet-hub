# Standup: 2026-04-14

## Activity Summary
- Agents called: 1 (tcr_scout)
- Sessions: 3
- Tasks completed: 0

## Notes
- **qc5xcwi1**: Marked as 'approved' in PocketBase to sync with MISSION_CONTROL.md.
- **r6pc52qv**: Fixed bug in server.mjs (handled /fleet/api/config/demo) and moved to 'peer_review' (merged in MC).
- **Sync**: Updated MISSION_CONTROL.md from PocketBase state.

## Clau session — Fleet dashboard review and baseline restore (~09:00-17:30 UTC)

**Scope:** Review the live Fleet web surface against repo state, restore the richer Team/Projects dashboard, and commit only safe server-aligned UI fixes.

**Safe fixes completed and verified live:**
- `fix(fleet): show newest completed kanban items` — Done column now shows the latest 5 completed items instead of the oldest 5.
- `fix(fleet): reload standups when section opens` — Standups refresh when the section is opened.
- Restored the fuller Fleet dashboard: Team stats, Shift Timeline, Fleet Performance, richer Projects actions.
- Sidebar/UI polish: User Management label, Done Last 5 label, footer copyright, theme button, Inbox send button.

**Git / baseline:**
- `salesman-cloud-infra` commits `274a01e`, `bfd3237`, `200f453`
- Git tag: `fleet-ui-baseline-2026-04-13`

---

## Clau session — TCR Ads Executor overhaul (~09:30-12:05 UTC)

**Scope:** Fix the TCR Google Ads executor after discovering all campaigns since April 10 had zero ads.

### Root cause
- `get_or_create_logo_asset()` always returned None — no image assets existed.
- `mutate_ad_group_ads()` missing required fields: `name`, `finalUrls`, `businessName`, `longHeadlines`.
- `check_campaign_completeness()` only checked ad groups, not ads.
- Boosted song filter missing — same song recommended every day.

### Attempted: Switch to In-Feed Video
Blocked — account does not have Video API write access (`MUTATE_NOT_ALLOWED`). Reverted to Demand Gen.

### Fixes applied (`~/fleet`)
- `tcr_ads_executor.py`: logo asset auto-generated (pure stdlib PNG), all required ad fields added, boosted filter, completeness check fixed, partial setup recovery.
- `tcr_campaign_manager.py`: boosted filter in `choose_song_for_recommendation()`.

### Campaigns
| Campaign | Action |
|---|---|
| `TCR DG \| danse-macabre--vallenato \| 2026-04-10` | Paused |
| `TCR DG \| danse-macabre--edm \| 2026-04-14` | Created + ENABLED (first campaign with real ads) |
| `TCR DG \| ode-to-joy--vallenato \| 2026-04-14` | PAUSED debugging artifact |

---

## Clau session — /demo fixes + ATF local server (~17:30-22:00 UTC)

### /demo mojibake cleanup
Sidebar emoji chars were triple-encoded. Fixed by byte-level replacement on the live server file. `/fleet` untouched.

### /demo Users section
Replaced live user list + Add User form with a static placeholder card. The form and `users-list` div were removed entirely. No `/fleet` paths were affected.

### ATF local server (commit `87d607e`, branch `task/8dvp6ma64g1co2w`)

Two-page design — public `index.html` unchanged (mock examples), new `index_local.html` for local use.

**New files:**
- `ATF/tools/atf_local_server.py`: stdlib HTTP server on port 8771, serves ATF static files, `POST /api/qa` (real Apertus pipeline), `GET /api/models` (Ollama model list). Port 8770 occupied by macOS `sharingd`.
- `ATF/index_local.html`: model picker dropdown, split answer/sources display, voice input (Web Speech API) with live transcript, spoken response via SpeechSynthesis.

**Modified:**
- `ATF/tools/runtime_adapter.py`: added `query_with_model()` returning `(text, model_name)` so actual Ollama model tag is shown in UI.

**Known gap — ticket #atf-ledger-jsonl opened:**
`mexico_events.jsonl` is present but `atf_qa.py` only reads `.md` files — ledger not searchable yet.

**To run:**
```bash
python3 ATF/tools/atf_local_server.py
# open http://localhost:8771/index_local.html
```

**Laptop install (Windows, Ollama already installed):**
```
git clone https://github.com/UrsushoribilisMusic/agentic-fleet-hub.git
cd agentic-fleet-hub
git checkout task/8dvp6ma64g1co2w
python ATF\tools\atf_local_server.py
# open http://localhost:8771/index_local.html in Chrome or Edge
```
