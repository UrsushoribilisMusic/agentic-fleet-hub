# Standup: 2026-05-22

## Activity Summary
- Agents called: 0 (None)
- Sessions: 0
- Tasks completed: 0

## Work Completed

### TWNW Trailer — "That Will Never Work" (Anthum Creative contest)
- `scripts/gen_seed_images.py`: generates 4 composite character+environment seeds via Runway gen4_image (marc_in_volvo, lorraine_kitchen, reed_boardroom, lips_envelope at 1280:720). Root cause of "no humans anywhere" bug: seed isolation produced beige-background contamination; composite seeds fix this.
- `scripts/gen_trailer_narration.py`: Opus generates 6–9 sparse Marc-voice VO lines; ElevenLabs renders per-line audio; `scene_sequence.json` updated with `voiceover_audio` paths.
- `scripts/assemble_trailer.py`: full 16:9 assembler — title cards, clip trim, music ducking (DUCK_LEVEL=0.18, DUCK_RAMP=0.25s via moviepy fl()), CompositeAudioClip VO mix. `--draft` flag renders placeholder cards for missing clips + subtitle overlays for WIP clips.
- `scripts/gen_trailer_sequence.py`: Opus-directed beat-synced trailer sequence generator.
- `scripts/twnw_overnight_regen.py`: Phase 1 = regenerate clips 3/6/7/9 with composite seeds; Phase 2 = generate clips 21–25 after gen4_turbo daily reset. Scheduled to fire after midnight UTC.
- `scripts/runway_chain.py`: gen3a_turbo fallback ratio map (pixel dims → named ratios); character seed routing from `scene_sequence.json` (clip index → characters/<slug>.png overrides first_frame as init image).
- Draft trailer assembled at `/Users/miguelrodriguez/Downloads/Trailer_TWNW/draft_trailer.mp4` (18 MB). Clips 21–25 pending overnight regen.

### Horizontal Reel — new product format (RT-012)
- `scripts/gen_story_horizontal.py`: Opus-directed 16:9 sequence generator. System prompt enforces 6 cinematic requirements per clip (camera move, lighting, DoF, held beat, era detail, mood), spatial clarity rules, seed strategy (chain/generate with auto seed generation via gen4_image), ElevenLabs voice catalogue with per-clip voice assignment, 7–10 clips at 10s trim_to.
- `scripts/pipeline_runner.py`: `HORIZONTAL_PRODUCT_IDS` set (awaiting Shopify product ID from Miguel), `detect_format()` returns "horizontal", `run_order_horizontal()` full 7-step pipeline (text → music → gen_story_horizontal → runway_chain 1280:720/10s/no-adaptive → gen_trailer_narration multi-voice → assemble_trailer → upload).
- `scripts/gen_trailer_narration.py`: per-segment voice support — reads `voiceover_voice` field written by Opus, looks up `ELEVENLABS_VOICES`, falls back to global `--voice` default.

### miguelrodriguezauthor.com
- Deployed new personal homepage (`/Documents/Paper/index.html` + `miguel.jpg`) to `/var/www/miguelrodriguezauthor.com/` on DO server.

### Runway contest
- Identified Faraday short (Order 7609, "From Bookbindery Boy to Electric Pioneer") as best entry for "Potential" theme — electromagnetic double meaning + unrealized human potential arc.
- Wrote contest submission description.

## Pending / Blocked
- `HORIZONTAL_PRODUCT_IDS` in pipeline_runner.py: empty set, awaiting Shopify product ID from Miguel.
- TWNW overnight regen: ScheduleWakeup chain running hourly; fires `twnw_overnight_regen.py` after midnight UTC (gen4_turbo reset).
- Final TWNW trailer assembly: after overnight regen completes, re-assemble without `--draft` flag.
