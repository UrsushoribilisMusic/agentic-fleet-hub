# Clau — 2026-05-17

## Task
RT-001 [P1]: pipeline_runner.py — detect product ID and pass format flag (fable|modern)

## Done
- Created branch `task/erwhktny` and wrote WORKLOG.md.
- Added `PRODUCT_FORMAT_MAP` dict to `scripts/pipeline_runner.py` — maps Shopify product IDs to `"fable"` or `"modern"`. Modern product ID is a commented placeholder until Shopify listing is created.
- Added `detect_format(order)` helper — reads `order["product_id"]`, looks up map, defaults to `"fable"`.
- `run_order()` now accepts `fmt=` override param; auto-detects from order if None; logs format and product_id at startup.
- Step 4 routes to `gen_story_modern.py` (modern) or `gen_story_assets.py` (fable).
- Step 8 routes to `assemble_modern.py` (modern) or `assemble_fable.py` (fable).
- `narrate_fable`, `upload_fable`, music selection: unchanged (shared across both paths).
- CLI gains `--format {fable,modern}` for local testing without a live order.
- Commit: `44128b5` on `task/erwhktny`. Task set to `peer_review`.

## Blockers
None.

## Next
RT-002 and RT-003 create the modern scripts (`gen_story_modern.py`, `assemble_modern.py`) that RT-001 now routes to.
