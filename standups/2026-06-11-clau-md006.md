# Clau — 2026-06-11

## Ticket
MD-006: Episode generator — four-block prompt → script/shotlist/voice/music-cue bundle

## What was done
Built the per-episode generation pipeline for the Microdrama project.

**Files created (microdrama repo, commit 2e03b16):**
- `generation/episode_schema.py` — `EPISODE_BUNDLE_TOOL` structured output schema for single-call generation of all 4 artifacts; en-GB voice defaults; validator (beat ±5s, chronological ordering, Runway staging, voice coverage)
- `generation/episode_generator.py` — four-block prompt assembler (CONTEXT + GRAMMAR + STYLE + CONSTRAINT), Haiku API call, output writer, CLI with `--episode`, `--episode all`, `--score-style`, `--voice-map`, `--lang`

**Smoke tests (en-GB Phase-1 arc, "The Gilded Accord"):**
| Episode | Time | Artifacts | Notes |
|---|---|---|---|
| ep01 — The Proposition | 44.8s ✓ | script.md + shot_list.json + voice_directives.json + music_cue_sheet.json | reversal_2 missing annotation (no reversal_2 in bible for ep1) |
| ep02 — First Night | 41.5s ✓ | all 4 files | beat ordering warning (cliff_setup @ 52s placed before peak_emotion @ 44s in script narrative) |

Both episodes under 90s target. All 4 output files generated. en-GB register confirmed.

## Status
Task → peer_review. Awaiting approval from peer agent.

## Known issues for Miguel's review

1. **Beat annotation ordering**: Model occasionally writes cliff_setup (52s) before peak_emotion (44s) in the script narrative because story-logic (setup → emotion) is inverted from timestamp order (emotion@44 → setup@52). Both timestamps still hit recipe card ±5s. Added explicit system prompt constraint for v2.

2. **Missing reversal_2 in ep01**: The bible's beat_episodes for ep01 has only 4 beats (no reversal_2). System prompt now requires all 5 beat types be annotated; model crafts a brief status-shift moment if the story doesn't provide one naturally.

3. **Voice IDs**: en-GB defaults use Charlotte/Daniel/Glinda ElevenLabs IDs. MD-009 will inject production voice IDs via `--voice-map`.

## Blockers
None.

## Next in queue
MD-008 (Phase-1 launch runbook) or MD-009 (bilingual twin render path).
