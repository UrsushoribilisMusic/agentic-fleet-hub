# Standup 2026-03-10

## Clau

### Done
- **Ticket #1**: `sheets_backend.py` — full Google Sheets read/write layer with offline queue (`pending_sync.json`)
- **Ticket #2**: `tracker.py` — mirrors all writes to Sheets after Excel save; startup sync on launch
- **Ticket #3**: `migrate_to_sheets.py` — one-time migration script (idempotent); ran successfully (261 rows already synced, 0 new)
- **Ticket #4**: `app.py` — analytics now loads from Sheets first (falls back to Excel); 5-min TTL cache prevents 4s API call on every platform switch
- **Ticket #5**: Server deployment — `api.py` (FastAPI, port 8001), `tracker_api.service` (systemd, auto-restart), `requirements.txt`, Caddy route wired (`api.robotross.art/tracker/*`). Service running and all 6 endpoints verified live.
- **Ticket #6 (OAuth wiring)**: OAuth client credentials handed off to Codi; salesman API `/auth/login`, `/auth/callback`, `/auth/verify` and `auth.js` cookie guard complete (Codi's work confirmed done)
- **Analytics heatmap**: Implemented negative-value encoding for untried piece×style combinations (negative = predicted avg; null = below 250-view threshold). Both `app.py` (local) and `server/api.py` (cloud) updated.
- **Codi GitHub deploy key**: Generated `ed25519` key pair on server, added `github-codi` alias to `~/.ssh/config`, user added deploy key with write access via GitHub settings — SSH auth verified.
- **MEMORY.md**: Updated with Sheets migration, tracker API, Command Center, and ticket status.
- **`command_center_api.md`**: Full API contract for Codi (all 6 endpoints, exact JSON shapes, UI section suggestions, heatmap notes, auth notes, deployment notes).

### Today
- Daily standup file

### Blockers
- None

---

## Codi

### Done (as reported)
- `/auth/login` → Google OAuth redirect
- `/auth/callback` → token exchange + sets `access_token` cookie
- `/auth/verify` → validates cookie, returns user info
- `auth.js` — cookie guard for all Command Center pages

### Pending (Ticket #7)
- Fix `renderHeatmap` bug (function called at main.js:888 but never defined — crashes JS, leaves all buttons dead)
- Build Analytics tab: style avg bar chart, piece avg bar chart, heatmap (Plotly, diverging scale: green=top performers, red=low, grey=untried below threshold, orange=predicted ≥250)
- Build Shorts tab: table + platform breakdown (YT / TikTok / Insta views per row)
- Build Videos tab: table sorted by YT views
- Build Story tab: table + bar chart comparing TT vs YT per chapter

### Blockers
- `renderHeatmap` bug blocks all other tabs (JS crash on page load)

---

## Infrastructure (afternoon session)

- **Codi deploy key #2**: Generated `ed25519` key pair on server (`/root/.ssh/codi_deploy_key2`), added `github-codi-salesman` alias to `~/.ssh/config`, user added deploy key with write access to `UrsushoribilisMusic/salesman-cloud-infra` — SSH auth verified.

## OpenClaw Assistant
- Done: Built the new command-center UI (stats/memory/daily tabs), added the analytics stack (charts/heatmap), introduced daily standup storage + UI, committed everything to `salesman-cloud-infra`, and pushed the changes.
- Today: Monitor Clau’s follow-up, add the next daily standup file once the JSON index updates, and keep an eye on OAuth/test issues.
- Blockers: None.
