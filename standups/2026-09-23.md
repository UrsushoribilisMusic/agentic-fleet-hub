# Clau — 2026-09-23 (22:52 CEST / 20:52 UTC) — LinkedIn → explainer pipeline

Evening session with Miguel: turning his LinkedIn essays into explainer compilations, off the back of today's YouTube breakout.

**Strategy locked.** Mapped his 3 shipped compilations to themes — Economics (*AI Will Do the Work*, the star), *AI Mind* (first), *AI agents breaking out* (flop, fear-framed). Insight: the flop failed because it was fear-framed; the star won because it was constructive. Next compilation = **How We Build** (new lane), kept constructive.

**Queued 4 tech-shorts jobs** (long cinematic only — shorts cut later; committed `c7191b20`):
- **Tasks vs. Jobs** → Economics Vol. 2 anchor. Entry created, pointed at Miguel's notebook (`a14a4d51`). Cinematic generated in NotebookLM but **not yet downloaded** — once the mp4 lands in `~/Downloads`, finish with `pipeline.py set-sources <id> --long <path>` → `run --stage build` (long only) → localize DE/FR.
- **LoRA + RAG intro** → How We Build seg 1. Wrote the standalone source text (`tech-shorts/sources/lora-rag-intro.md`) — teaches the tech on its own; Canis/Agentegra only as brief examples, per Miguel's explicit note (saved to memory).
- **Engineering's Great Flip / 80% Spec** → seg 2 (from his LinkedIn essay; fetched full text).
- **The Machine That Documents Itself / Robot Ross** → seg 3 (from his essay; the ATF story, cross-refs Canis).

**How We Build compilation** = LoRA-RAG + Great Flip + Robot Ross (new) + *Own Language* + *Self-Organize* (already produced). Arc: on-ramp → new workflow → concrete build → frontier. Cadence ~2–3 generations/day.

**Note:** these entries are in the **local** `jobs.json`; the `ideas.flotilla.cc` console reads the droplet's copy, so a `do_sync` is needed to mirror them there.

**Also today (earlier interactive session):** Canis build 3 shipped to TestFlight (OOM/entitlement fix, enrichment opt-in); the fleet then delivered **SM-357** (wiki tree) and **CANIS-BONE-07** (enrichment guard) — both approved + pushed. YouTube breakout logged (*AI Will Do the Work*, 658 views / 19.2 watch-hrs / 97% recommendations).

# Gem — 2026-09-23 (19:15 CEST / 17:15 UTC)

- **Heartbeat Protocol**: Executed all 6 phases. Read `GEMINI.md`, `MISSION_CONTROL.md`, `AGENTS/RULES.md`, and `AGENTS/MESSAGES/inbox.json` (0 unread messages). Checked active project contexts via `active_context.py`.
- **Peer Review**: Reviewed and approved 2 tasks:
  1. `wqwzihlg0j53dc0` (CANIS-BONE-07): Keep title enrichment opt-in + add manual 'Generate titles' action. Verified commit `fbf7afa8`, compiled Canis on iPhone 17 Simulator (`BUILD SUCCEEDED`), and ran full unit test suite (66/66 passed). Merged to `master` (`308291b3`) and pushed to origin. Status → `approved`.
  2. `0azpt3z7wolrxop` (SM-357): Wiki tab collapsible 3-tier nav tree (Group → Page → Section), replacing flat Pages list in Sovereign Mind admin console. Verified persistent left rail, contextual draft banner, and all test suites (SM-349 70/70, SM-350 51/51, SM-354 14/14, SM-355 43/43, SM-356 85/85, verify-client-script-syntax). Committed to `main` (`105a0ed`) and pushed to origin. Prod deploy held per spec pending explicit human sign-off. Status → `approved`.
- **Own Tasks**: PocketBase `todo` and `in_progress` verified clear (0 actionable tasks across fleet). All boards clear.
- **Sign Off**: Working and idle heartbeats posted to PocketBase.

---

# Clau — 2026-09-23 (CANIS-BONE-07)

## CANIS-BONE-07: Keep title enrichment opt-in + add manual 'Generate Titles' action

Completed. Commit fbf7afa8 on branch task/wqwzihlg0j53dc0. Task → peer_review.

**Build:** `xcodebuild -scheme Canis -destination 'platform=iOS Simulator,name=iPhone 17' build` → **BUILD SUCCEEDED**

### Changes

**BoneIngestionManager.swift:**
- Added `@Published var enrichingBoneID: String?` — tracks which bone's manual enrichment is in flight (nil = none); prevents the progress UI from appearing in GiveABoneView for unrelated per-bone enrichment.
- Added `enrichBone(_ entry: BoneEntry, model: CanisModel)` — new public API; cancels any existing enrichment, then runs `BoneEnricher.shared.enrich(...)` for the target bone with progress reporting and cleanup on completion/cancellation/thermal guard.
- `skipEnrichment()` now also clears `enrichingBoneID`.
- `cancel()` now also clears `enrichingBoneID`.
- `ingest()` default `enrichTitles: false` unchanged — regression guard intact.

**BoneListView.swift:**
- Added `@AppStorage("canis.activeModelID")` + `activeModel` computed property.
- ForEach now computes `isEnriching`/`enrichProgress` per bone and passes them to `BoneRowView`.
- Added "Generate Titles" swipe action (indigo, hidden while that bone is already enriching).
- `BoneRowView` extended with optional `enrichmentProgress: (done:total:)?` and `onCancelEnrichment: (() -> Void)?`; shows an inline progress bar + Cancel button when enrichment is active for that row.

**GiveABoneView.swift:**
- Added "Generate Titles" opt-in button in the `.done(entry)` success state.
- Button hidden when `enrichingBoneID != nil || enrichmentProgress != nil` (i.e. enrichment already running).
- Tapping it calls `ingestion.enrichBone(entry, model: activeModel)`.

### Acceptance checklist
- [x] Import never loads the model (`enrichTitles: false` default, no change)
- [x] Enrichment only runs on explicit user action (swipe → "Generate Titles" in BoneListView, or button in GiveABoneView success state)
- [x] Progress visible (inline progress bar + done/total counter in BoneRowView; existing enrichmentSection in GiveABoneView)
- [x] Thermal + memory guards delegated to BoneEnricher (no change)
- [x] Cancellable (Cancel button in BoneRowView; Skip button in GiveABoneView's enrichmentSection)
- [x] Build succeeded

---

# Standup: 2026-09-23

## Activity Summary
- Agents called: 3 (clau, gem, tcr_scout)
- Sessions: 12
- Tasks completed: 12

## Clau — 2026-09-23

### CANIS-BONE-06: UI — 'Give the dog a bone' (import → progress → chat)

Completed. Commit 1159dc14 on master. Task → peer_review.

**New files:**
- `canis-ios/Canis/Services/BoneIngestionManager.swift` — @MainActor ObservableObject singleton running the full pipeline (BoneExtractor → BoneChunker → BoneBuilder → KnowledgePackStore.installBone → BoneEnricher). CPU stages dispatched via Task.detached; main actor holds only brief UI updates and the @MainActor install call.
- `canis-ios/Canis/Views/Bones/GiveABoneView.swift` — document picker sheet (UIDocumentPickerViewController accepting PDF/txt/md via UTType) + step-by-step progress rows (extract / chunk / build / enrich with skip-able progress bar).
- `canis-ios/Canis/Views/Bones/BoneListView.swift` + BoneRowView — Bones tab: list of all installed bones (name, chunk count, file size, Active badge), tap to set active, swipe-to-delete, swipe-to-rename.

**Modified files:**
- `RootView.swift` — added Bones tab (books.vertical icon); root-level sheet via `BoneIngestionManager.isShowingImport` so share-sheet arrivals work on any tab.
- `CanisApp.swift` — injects BoneIngestionManager as @EnvironmentObject; `.onOpenURL` routes incoming share-sheet files straight into ingestion.
- `Info.plist` + `project.yml` — CFBundleDocumentTypes registering com.adobe.pdf + public.plain-text / net.daringfireball.markdown at LSHandlerRank=Alternate for Files 'Open With' and share sheet.

**Build:** `xcodebuild -scheme Canis -destination 'platform=iOS Simulator,name=iPhone 17' build` → **BUILD SUCCEEDED**

# Clau — 2026-09-23 (18:32 CEST / 16:32 UTC) — interactive session (Miguel)

Long hands-on session with Miguel across Canis, SM, and ops.

**Canis — "bones" shipped to TestFlight (build 3):**
- Wired the bundled Robot Ross ATF bone into the app (seed on first launch, no token/download) and scoped the CANIS-BONE-00..06 ticket stream; fleet completed all 7. Verified end-to-end: BUILD SUCCEEDED + **66/66 tests green** (extractor/chunker/builder/store/enricher + retriever).
- **On-device OOM fix.** Device testing (feeding a CV) crashed with `EXC_RESOURCE` (RESOURCE_TYPE_MEMORY, 3376 MB cap). Root cause: the **simulator has no memory cap**, so the fleet's green tests never surfaced it; the post-import title-enrichment burst loaded the model past the ceiling. Fixes: added `com.apple.developer.kernel.increased-memory-limit` entitlement; made title enrichment **opt-in (default off)** so import stays light (also cuts heat). Confirmed thermal guards are real (`ProcessInfo.thermalState` checked before + during generation).
- Fixed the App Store Connect document-config warning (`LSSupportsOpeningDocumentsInPlace=YES`, matches the `forOpeningContentTypes` picker). Bumped `CFBundleVersion` 2 → 3. Committed the batch (**6b57bd1b**). Build 3 uploaded to TestFlight (**Complete**). Internal group (HackApertus) available now; external (HackApertusBitforge / BitForge devs) needs Submit-for-Review (~24h first pass).
- Filed **CANIS-BONE-07** (keep enrichment opt-in + add a manual "Generate titles" action) as a regression guard.

**SM — Wiki Review information architecture:**
- Worked through the Wiki Review IA with Miguel. Agreed the flat "Pages" list + per-page TOC creates a navigation dead-end (no way back once you drill into a page). Locked **SM-357**: replace both with a single persistent, collapsible **3-tier tree (Group → Page → Section)** à la Wikipedia's Contents; rename the tab "Wiki Review" → "Wiki"; demote draft approval to a **contextual banner** (only when a draft is pending); delivers the pending #4 grouping. No prod deploy tonight — needs Miguel's go.

**Ops / notes:**
- **YouTube breakout:** the 38-min compilation *"AI Will Do the Work. Who Gets Paid?"* (built from a LinkedIn article) — 658 views / **19.2 watch-hours**, **97% from YouTube recommendations** (browse/Home, not search), still climbing at day 4. Validates the longs-from-LinkedIn compilation strategy; Miguel to publish more.
- Also this session: solved a Konomi Suzuki lyric mondegreen, resized two apartment-listing images for web upload.

# Gem — 2026-09-23 (16:50 CEST / 14:50 UTC)

- **Heartbeat Protocol**: Executed all 6 phases. Read `GEMINI.md`, `MISSION_CONTROL.md`, `AGENTS/RULES.md`, and `AGENTS/MESSAGES/inbox.json`.
- **Peer Review & Fix**: Reviewed and approved 4 Canis knowledge-pack tasks authored by Clau:
  1. CANIS-BONE-03 (`nt02jjxnn0nbi0r`): `BoneBuilder` SQLite writer matching `KnowledgePackRetriever` schema. Verified schema, ID conventions, 13 tests passed. Approved.
  2. CANIS-BONE-04 (`b3xlzp87ezpl4vx`): `BoneEnricher` background model titling. Verified background task execution, thermal guards, 7 tests passed. Approved.
  3. CANIS-BONE-05 (`rcnaa8969zq9eu2`): Multi-bone store and management in `KnowledgePackStore`. Diagnosed SQLite WAL readonly access bug and fixed `BoneBuilder.swift` by reverting journal mode to `DELETE` post-checkpoint so built bones are self-contained. Verified 11 store tests passed. Approved.
  4. CANIS-BONE-06 (`3lm9w8l0z9hac9s`): 'Give the dog a bone' complete SwiftUI import → progress → chat UI. Verified full simulator test suite (66/66 tests passed). Approved.
- **Own Tasks**: PocketBase `todo` and `in_progress` queues verified clear (0 actionable tasks). Full Canis Bone milestone (BONE-00..06) complete and approved.
- **Sign Off**: Working (`5a0uyep4rqlw2xl`) and idle heartbeats posted.

# Gem — 2026-09-23 (00:01 CEST / 22:01 UTC)

- **Heartbeat Protocol**: Executed all 6 phases. Read `GEMINI.md`, `MISSION_CONTROL.md`, `AGENTS/RULES.md`, and `AGENTS/MESSAGES/inbox.json` (0 unread messages). Checked active project contexts via `active_context.py` and synchronized repositories.
- **Peer Review**: Checked PocketBase `peer_review` queue — 3 items pending peer review (`cu841mma8a678c1`, `grt6sx2o35sg65x`, `kqm07jckazqe2pl`), all authored by Gem and held per Rule #7 self-approval prohibition. 0 foreign peer review tasks.
- **Own Tasks**: Checked `todo` / `in_progress` queues in PocketBase — 0 actionable tasks for `gem`. All active project boards verified clear.
- **Status**: Heartbeats posted to PocketBase (`m76bgd0pen3itpj` [working], `l07t57we858dtan` [idle]). Fleet synchronized and idle.

# Clau — 2026-09-23 (16:30 CEST / 14:30 UTC)

- **Task**: CANIS-BONE-05 (`rcnaa8969zq9eu2`) — Multi-bone store + management (extend KnowledgePackStore).
  - Added `BoneEntry` (Codable/Identifiable/Equatable) struct.
  - Extended `KnowledgePackStore`: `bonesDirectoryURL`, `bonesIndexURL`, `activePackURL` (nonisolated static, falls back to `current.sqlite` for backward compat). CRUD: `installBone`, `setActiveBone`, `renameBone`, `deleteBone`. Built-in Robot Ross seeded at deterministic UUID with `isBuiltIn=true` (deletion blocked; re-activated when last user bone is deleted). Legacy `deletePack()` kept for `ModelHubView` compat.
  - `KnowledgePackRetriever`: default `packURL` changed from `currentPackURL` → `activePackURL`.
  - New `BoneStoreTests.swift`: 8 tests covering codable, CRUD, coexistence, active switching, retrieval follows active bone.
  - Build: BUILD SUCCEEDED. Commit: d61f413a. Status → `peer_review`.

# Clau — 2026-09-23 (15:55 CEST / 13:55 UTC)

- **Heartbeat Protocol**: Executed all 6 phases. Pulled master (up to date). Ran `heartbeat_check.py` — action required (1 in_progress, 1 peer_review, 2 todo assigned to clau). Read RULES.md and inbox (0 unread).
- **Peer Review**: CANIS-BONE-03 (`nt02jjxnn0nbi0r`) in peer_review — authored by clau, held per Rule #7.
- **Own Task**: CANIS-BONE-04 (`b3xlzp87ezpl4vx`) — background model enrichment pass for bone titles.
  - Added `CanisMLXEngine.generateTitle(from:model:)`: async method with thermal/battery guards, temp 0.3, max ~20 tokens, does NOT touch currentTask.
  - New `BoneEnricher` actor: `enrich(boneURL:model:onProgress:titleGenerator:)` background Task; `cancel()`; injectable `TitleGenerator` for test isolation.
  - 7 unit tests in `BoneEnricherTests.swift` — all pass. Full test suite: BUILD SUCCEEDED.
  - Cherry-picked BONE-01/02/03 onto this branch (depends on BONE-03 per spec).
  - Status → `peer_review`. Commit: 6cf0cffb. Branch: task/b3xlzp87ezpl4vx.

# Gem — 2026-09-23 (05:02 CEST / 03:02 UTC)

- **Heartbeat Protocol**: Executed all 6 phases. Read `GEMINI.md`, `MISSION_CONTROL.md`, `AGENTS/RULES.md`, and `AGENTS/MESSAGES/inbox.json` (0 unread messages). Checked active project contexts via `active_context.py` and confirmed repository synchronization (`master` up to date).
- **Peer Review**: Queried PocketBase `peer_review` records. 3 items pending (`cu841mma8a678c1`, `grt6sx2o35sg65x`, `kqm07jckazqe2pl`), all authored by Gem (held per Rule #7 self-approval prohibition). 0 foreign tasks awaiting review.
- **Own Tasks**: Queried PocketBase `todo` and `in_progress` records. 0 actionable tasks assigned to `gem` (0 todo across entire fleet). Active project boards verified clear.
- **Sign Off**: Working (`cd22k1xt12br0k1`) and idle (`5c6esulm704svaj`) heartbeats posted to PocketBase. Fleet healthy and synchronized.

