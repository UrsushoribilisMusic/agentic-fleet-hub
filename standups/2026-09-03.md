# Standup: 2026-09-03

## Activity Summary
- Agents called: 2 (gem, tcr_scout)
- Sessions: 3
- Tasks completed: 0

## Interactive session (Clau) — FinOps dashboard overhaul

**Root-caused and fixed the FinOps "Financial Ops" dashboard** (the Flotilla SPA at
`api.robotross.art/fleet/`, served by `salesman-cloud-infra` `server.mjs` + `fleet/dashboard.html`).
First edited the wrong dashboard (`fleet/package/dashboard/engineering/` — an unused variant); the
live one is `fleet/dashboard.html` + `fleet/assets/main.js`.

**Data correctness (server.mjs + classical-remix.mjs):**
- **Revenue was fake CHF 10** — the Classical Remix "Revenue 30d" fell back to `income_ledger` (a
  stale DistroKid row). Now sourced from `channel_revenue_daily` (real YouTube AdSense, ~CHF 0.9/30d).
  Added `channel_revenue_daily` to `SNAPSHOT_FINANCIAL_COLLECTIONS` (it was pushed but dropped).
- **P&L YouTube AdSense** line now populated (was CHF 0) from the same source.
- **Active campaigns showed 1 (Celtic)** from a stale 2026-07-13 Google Ads snapshot. Added a
  10-day staleness guard in `classical-remix.mjs` so an un-refreshed snapshot can't show a campaign
  as live → now 0. (Root cause: `cr_ads_snapshot` cron stopped after 2026-07-13.)

**Classical Remix layout:** consolidated stats into one box, Channel Stats + Last 5 Days side-by-side
with the chart full-width below, dropped Hours-remaining/Projected-YPP (past 4,000h). **Phases fixed
retroactively**: P6 complete → P7 active, dated to the real 4,000h crossing (2026-07-16, from the
watch_hours_ledger).

**Tech Shorts — rebuilt on the token-free "compute on Mac → push" mechanism (per Miguel):**
- The tab had **no JS loader at all** (`loadFinancialTechShorts` was referenced but never defined) —
  every field sat at its default dash. Wrote it.
- Moved off the droplet's live YouTube call. `stats_sync.py` (Mac, holds the token) now also captures
  **published_at** (persisted + sortable, newest-first) and **per-video revenue** joined from
  `video_revenue_overlay.json`. Droplet `buildTechShortStats` now reads the Mac-pushed jobs.json only
  (`youtube_live:false`).
- Removed the "YouTube Revenue" box (tech-shorts ad revenue is part of the Classical Remix channel);
  added an "Ad Revenue" box + a per-video **Revenue** column (sub-cent "microshekels", 4dp).

**Other:**
- `fleet.push` cadence 60s → **hourly** (`FLEET_SYNC_INTERVAL_SEC=3600`).
- **DistroKid income recorded**: corrected stale CHF 10 → USD 18.16 (Apr–May) + added USD 27.11
  (Jun–Jul, partial — July YouTube ContentID delayed). Apr–Jul total **USD 45.27** (ContentID $39.78
  is the biggest music earner, dwarfing channel AdSense ~$1).

**Next:** add a **Year-to-Date** view to FinOps (surfaces cumulative income like DistroKid that the
30-day P&L can't show).
