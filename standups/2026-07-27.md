# Standup: 2026-07-27

## Activity Summary
- Agents called: 2 (gem, tcr_scout)
- Sessions: 4
- Tasks completed: 0

## Notes
Fleet agents idle. Miguel + Dev Groar: SiliconOracle submitted to the App Store, and a Sovereign Mind landing-page update shipped to sm.flotilla.cc (see below).

---

## Miguel + Dev Groar Session — SiliconOracle App Store submission

- **Submitted v1.0.0 / build 21 + IAP to App Review** — "2 Items Submitted." Up to 48 h review. Release setting: **auto-release after approval, no earlier than Jul 29 2026** (aligns with the Apertus event).
- **Unblocked a stuck IAP submission.** The IAP `cc.flotilla.siliconoracle.unlock.full` was stuck in **"Developer Rejected"** (leftover from the removed build-16 submission), which greyed out "Add for Review" everywhere with no tooltip and every app-level field already complete. Fix: open the IAP → force a change (edit Reference Name by a char / re-upload the review screenshot) → **Save** flips it "Developer Rejected" → "Ready for Review" → App Store Connect auto-creates a **Draft Submission** → add the app version via the version page's "Add for Review" → both items land in the draft → **Submit**. (Key rule: a first IAP must be submitted *together with* an app version.)
- **Final shipping config:** free app, Qwen default, Ministral + Apertus Nano (experimental) text options, tip-gate IAP unlock.

**Next:** await Apple's review email (up to 48 h). Approved → auto-releases on/after Jul 29. Rejected → address reviewer note and resubmit.

---

## Miguel + Dev Groar Session — Sovereign Mind landing page (sm.flotilla.cc)

All changes in `~/projects/sovereign-mind/server.js` (the page is server-rendered inline in `server.js`, not a static file). Commit `a88fc0c` on branch `task/5topq6g3aj8btmc-sm309`.

- **Hero CTAs.** "Take the submarine test" renamed to **"See the submarine test in action"** and repointed from the in-page `#try` anchor to the YouTube short `https://youtube.com/shorts/bhLfaAd_xHE`. Added a third hero button **"View a sample wiki"** → `https://api.robotross.art/atf/`. `.hero-cta` already wraps, so three buttons reflow on mobile.
- **New `#learns` section — "The system learns from experience."** Sits between How-it-works and Why-us. Two blocks: (1) the three parts of the on-device stack — local open-weights model, role-specialised prompt (service engineer / production manager), and the RAG; (2) the feedback loop "From log file to wiki page" — large model mines the machine's log files for recurring failure modes, drafts new wiki pages grounded in the logs *and* the existing wiki, which are reviewed and recompiled onto the device.
- **New flow diagram** in that section: `INPUT compiled wiki → PROCESS parse the log files → OUTPUT enhanced RAG (FAQ built in)`. New `.flow` CSS; stacks vertically with rotated arrows under 900 px.
- **Mistral hackathon references removed** from that section at Miguel's request (eyebrow is now "Beyond the manual"). `grep -i hackathon server.js` → 0 hits.
- **Contact section trimmed** to just the form: removed the founder / company / location / email `<dl>` and the three enquiry blurbs (Enterprise Trials, Investor Enquiries incl. the CHF 250k seed figure, Partnership & Distribution), plus the now-dead `.contact-meta` CSS. Form centred via new `.contact-grid-solo`. Miguel's email survives only in the form's failure fallback message.

**Deploy:** backed up remote to `server.js.bak-20260727`, `scp` to `robotsales:/opt/sovereign-mind-backend/server.js`, `systemctl restart sovereign-mind-backend.service`. Verified live: HTTP 200, new markup present, zero `contact-meta` / `hackathon` hits. Remote had been identical to local HEAD beforehand, so no drift was clobbered.

**Open (not done, deliberately):** no nav link for the new `#learns` section — nav is still Problem / How it works / Why us / Contact.
