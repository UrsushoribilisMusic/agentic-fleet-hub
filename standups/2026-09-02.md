# Standup: 2026-09-02

## Activity Summary
- Agents called: 2 (gem, tcr_scout)
- Sessions: 6
- Tasks completed: 0

## Interactive session (Clau)

**CANIS disposition paper — finalized and SUBMITTED to arXiv (`submit/8026681`, stat.ML).**
Title: "Reading Disposition from Smartphone-Class Open Models: A Forward-Only J-Space Probe on
Apertus-4B, Ministral-3B and Qwen3-4B."
- **Three-rater human IRR** completed (R1/R2/R3, anonymized; Fleiss κ = 0.42) + **four-model
  cross-family judge panel** (Gemini/Claude/Codex/Mistral) rated the same 50 items via PocketBase
  tickets → **Fleiss κ = 0.89**. The 0.89-vs-0.42 gap is the paper's spine.
- **Bootstrap 95% CIs** (`compute_bootstrap_ci.py`, seed 20260902): human 0.42 [0.30–0.53], model
  0.89 [0.81–0.96], gap 0.47 [0.35–0.59] (non-overlapping). Folded into abstract + §5.2.
- **Author-as-rater disclosure** in §7 (author = R2, κ 0.56; human-without-author = 0.47 [0.29–0.63]).
- **Data Availability** section made real: calibration vectors + both prompt sets + per-item scores
  bundled as ancillary files (`eval/anc/` + README manifest, ~11 MB). Built the arXiv submission
  tarball from Big Sis's LaTeX package (main.tex + refs.bib + main.bbl + 3 figures at root + anc/).
- **Paper live** at canis.flotilla.cc/reading-disposition.pdf, linked from the main page (nav +
  Deep Tech callout) for grant reviewers (Prototype Fund).

**Tech-shorts — two new videos produced end-to-end + published; pipeline "missing short" fixed.**
- Root-caused why improving-alignment failed to build: NotebookLM's **Short** format was never
  generated, so the pipeline had no `raw_short` input (Miguel's diagnosis was right). Fix = generate
  BOTH Cinematic + Short in NotebookLM, `set-sources`, `set-copy` (hook), `run build` (ElevenLabs VO
  + ffmpeg), `run upload`.
- **The Anatomy of an AI Breakout** + **The Escalation of a Cheat** (reward-seeking): each now a
  proper assembled short+long (hook card + VO + CANIS outro), uploaded, made public. Raw draft longs
  deleted (superseded). Rich keyword-dense descriptions drafted (METR/OpenAI/Anthropic/HackerOpus).
- **Published both to X** via the flotilla publisher (2-line hook + company/incident hashtags,
  tracked). Note: `x_client.py` default secret path is `/flotilla/x` but the secrets live at `/` —
  override `X_SECRET_PATH=/` (as stats_sync does); worth reconciling in the client.

**Milestones:** first YouTube ad revenue (~$1.03). LinkedIn article "Spontaneous altruism in an LLM
collective" published (feedback given: strong exam-analogy framing; flagged the anthropomorphism
caveat that ties to the CANIS IRR finding + a CANIS CTA).

**Stats dashboard "enrichment gone AGAIN" — root-caused + durably fixed.** Backend was fine
(121/125 enriched, files fresh); the browser was serving a **stale cached `/tracker/videos` JSON**
because the endpoint sent no `Cache-Control`. Added a `no-store` middleware to the tracker API
(`salesman-cloud-infra` api.py, deployed) so a stale enrichment snapshot can never be cached again.
Hard-refresh clears any already-cached copy.

**Note:** droplet-side edits (canis PDF, dashboard, SM auth from 09-01) are deployed but the SM
`server.js` fixes remain droplet-only (no Mac repo mirror).
