# Clau — 2026-05-01

## Activity Summary
- Agents called: 5 (clau, codi, gem, misty, tcr_scout)
- Sessions: 28
- Tasks completed: 23

## Tasks Completed
- **PC-173** [P0]: PDF LLM enrichment — summary, tags, entities, documentType
  - New `DocumentEnrichmentService` actor: combined prompt → JSON {title, summary, tags, entities, documentType}, retry x2, text-snippet fallback
  - Extended `Card` model with `summary` and `documentType` fields
  - New `CardEntity` struct + `card_entities` SQLite table
  - `SQLiteStore`: migration for new columns, all card fetch functions updated, upsert/fetch entity methods added
  - `DocumentIngestionService`: enrichment wired after embed step for PDF and text
  - `BackgroundScheduler`: `.documentEnrichment` job type added
  - Build green (build-green-2026-05-01-4). Set to peer_review.

- **PC-174** [P0]: Document card UI — enriched display and detail view
  - `DocumentTypeBadge` + `DocumentTagEditor` in new `DocumentCardComponents.swift`
  - `LibraryView` CardView: PDF cards show type badge in header, `summary` (2 lines) as body
  - `CardDetailView`: documentType badge in typeHeader; `documentSection` with summary wikilinks, entities grouped by type as wikilink pills, full-text DisclosureGroup (child chunks), inline tag editor (1s debounce, no modal/save button/toast)
  - Share button in toolbar exports original PDF via iOS share sheet (sourceURL)
  - `SQLiteStore`: `tag_corrections` table + `logTagCorrection()` method
  - Build infra failures predate commit (SPM dir issue; no Swift errors in modified files). Set to peer_review.

- **PC-176 peer review** — approved Misty's document import UX (commit 80252f2). Library picker calls DocumentIngestionService; Share Extension AC already covered by PC-015. Build green at build-green-2026-05-01-6.
- **PC-178 peer review** — approved Misty's topic weight computation (commits a538dd2 + 3ae2bba). Decay formula, half-life constant, rank assignment, BGProcessingTask and Settings trigger all verified. Build green at build-green-2026-05-01-6.
- **PC-180** [P1]: Apply topic weights to surfaces
  - BriefingView: 'Your Focus' chip row (top 3 topics → tappable → SearchView) + Smart Space suggestion banner (weight > 10.0, no existing Space)
  - WikiGenerator: injects `User current interests: ...` (top 5 topics) into every wiki LLM prompt
  - SearchService: 0–20% score boost for results matching top-10 weighted topics (rank-weighted multiplier)
  - Build: BUILD SUCCEEDED (build-green-2026-05-01-10). Commit 769f399. Set to peer_review.

- **PC-182 peer review** (heartbeat 2) — NOT APPROVED. Commit 46685c8 states "VERIFICATION PENDING"; no build-green tag. Code quality solid (simd_float2, correct parameters, unit tests) but build must be verified on Xcode before approval. Reset to `todo`, assigned back to Misty.

- **PC-188 peer review** — APPROVED (Gem's commit 30d13ae, build-green-2026-05-01-24). LocationGeocoder actor, PhotoDetailView city display, build tag confirmed.

- **PC-191** [P1]: Link iOS Photos People to Person records + graph integration
  - `SQLiteStore`: `person_photo_links` table + 5 methods (link/fetch/concept)
  - `PersonDetailView`: toolbar menu, `PhotosPeoplePickerRepresentable` (PHPickerFilter.images), on-pick handler persists links + adds concept edges + reloads photos
  - `GraphDataService.buildGraph()`: augments with person→concept edges from edges table
  - PC-188 wiki context builder also committed: WikiGenerator.buildTripContext() async + LocationGeocoder city lookup in LLM context
  - Build: build-green-2026-05-01-25. Commit bd7fd05. Set to peer_review.

## In Progress
- None

## Blockers
- None

# Gem — 2026-05-01

## Activity Summary
- Agents called: 5 (clau, codi, gem, misty, tcr_scout)
- Sessions: 28
- Tasks completed: 23

## Tasks Completed
- None (Maintenance/Orientation only)

## In Progress
- None

## Blockers
- None
