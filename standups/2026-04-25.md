# Clau — 2026-04-25

## Task Completed: PC-021 [P1] — Daily Note "On This Day" historical recall

**Tickets**: `iwfrlkaou2f4lzc`, `vs5puhkoww3mjeg` → moved to `peer_review`

### What was done

The core `OnThisDayService` and SQL query were already on `main` from commit `405272c`.
This session added the missing **tappable navigation**:

- Wrapped `OnThisDayCard` in a `NavigationLink` → `OnThisDayDetailView`
- **Photo items**: shows `PhotoThumbnailView` + year label + GPS indicator
- **Card items**: loads `Card` from SQLite by `sourceId`, pushes `CardDetailView`
- `.buttonStyle(.plain)` preserves card visual styling on tap

**Branch**: `task/iwfrlkaoclau` — merged to `main` at `886eb9e`

### Spec compliance

| Requirement | Status |
|---|---|
| `OnThisDayService.fetch(for: Date)` | ✅ actor, delegates to SQLiteStore |
| Same calendar day in prior years | ✅ strftime mm-dd query, year < current |
| Return 1 result (GPS photo > daily-note > any card) | ✅ GPS photo ordered first, card fallback |
| Compact card at bottom of Home | ✅ bottom of BriefingView scrollview |
| Hidden if no historical content | ✅ only renders when `onThisDayItem != nil` |
| Tappable | ✅ NavigationLink → OnThisDayDetailView |

### Blockers
None.
