# Codi — 2026-06-20 / Clau — 2026-06-20

## Activity Summary
- Agents called: 1 (codi)
- Sessions: 2
- Tasks completed: 0

## Completed
- DW-001 local repo skeleton implemented at `/Users/miguelrodriguez/projects/dental-domain-kit`.
- Added `Makefile` targets for `sources`, `ingest`, `wiki`, `primitives`, and `serve`.
- Added stub `sources.yaml`, empty article, corpus/wiki/primitives/diagrams/scripts layout, provenance/demo docs, and stdlib-only scripts.
- Verified `make sources && make ingest && make wiki && make primitives`.
- Verified `make serve` started a local wiki server and returned HTTP 200 for `/`.
- Pushed `main` and `task/xhgx8r771rrfd6i` to `github.com/UrsushoribilisMusic/dental-domain-kit`.
- Posted PocketBase output and moved DW-001 to `peer_review`.

## Blockers
- None.

## Notes
- Local commits in `dental-domain-kit`: `76842b6` (`WORKLOG.md`) and `0ea2cdd` (skeleton pipeline).
- Local remote is named `github-clau`; it uses the clean HTTPS repo URL because the SSH alias is absent in `~/.ssh/config`.

---

# Clau — 2026-06-20

## Completed
- **DW-006** [P1]: Licensing pass — PROVENANCE.md + reference-mode summaries
  - Populated `sources.yaml` with all 6 sources from the DW-002 spec; `make sources` validates 6 entries clean.
  - Authored `PROVENANCE.md`: ingest table (MepV, EU MDR, Swissmedic) + own-words reference summaries (SNOMED CT/SNODENT, ISO dental standards, SSO DENTOTAR®). No licensed text reproduced; no excerpt exceeds 25 words.
  - SSO tariff licence researched (2026-06-20): DENTOTAR® PDFs are on sso.ch but carry no open licence; SSO copyright statement explicitly reserves commercial reproduction rights. Mode stays `reference`. Ingest requires written SSO data-use agreement.
  - Committed `ec432eb` to `task/6jqumsn4wapehqa`, pushed to `dental-domain-kit`.
  - Task patched to `peer_review`.

## Blockers
- None.

## Notes
- sources.yaml population slightly overlaps DW-002 (Codi) scope. DW-002 still needs `make sources` CI wiring — sources list is now in place.
