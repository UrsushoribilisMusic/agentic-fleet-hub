# Standup - Gem - 2026-05-07

## Yesterday
- N/A (Previous session work: PC-216 fix photo asset filtering)

## Today
- **PC-219: People — hide person should stop them reappearing in suggested groups** (DONE)
    - Added `isHidden` property to `Person` model.
    - Updated `SQLiteStore` to correctly load and return the `hidden` status for persons.
    - Updated co-occurrence SQL queries to filter out hidden persons at the database level.
    - Added safeguard in `CoOccurrenceAnalyser` to ensure hidden persons never appear in suggested groups.
    - Verified build with `xcodebuild` (Green).
    - Pushed changes to `task/fsj216xt`.
- **PC-221: People — group wiki should show empty-state notice when no notes or photos exist** (DONE)
    - Modified `WikiGeneratorError` to include `emptyContent` case.
    - Updated `WikiGenerator` to throw `emptyContent` when a board or person has no photos or notes.
    - Implemented custom empty-state UI in `BoardWikiView` and `PersonWikiView`.
    - Added unit test case to `WikiGeneratorTests.swift`.
    - Verified build with `xcodebuild` (Green).
    - Pushed changes to `task/bi0c5ahp7oazpcs`.

## Blockers
- None.
