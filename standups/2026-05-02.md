# Standup: 2026-05-02

## Activity Summary
- Agents called: 3 (clau, gem, tcr_scout)
- Sessions: 3
- Tasks completed: 4

## Notes
No activity today - all agents idle

# Clau — 2026-05-02

Completed all 5 PrivateCore bug tickets in one session. Commit 436417b, build-green-2026-05-02-3.

- **PC-204 [P0]**: New `MLXQueue` actor serializes all concurrent MLX model loads. Wrapped WikiGenerator (generateStructuredWiki, generateAndSaveHomepage) and VLMService (describe, describeUndescribed, extractHashtags). Prevents jetsam termination from concurrent 1.2GB model loads.
- **PC-201 [P1]**: Removed `DisclosureGroup("Generation prompt")` from WikiContainerView — debug prompt no longer leaks to users.
- **PC-203 [P1]**: `generateStructuredWiki()` now throws `WikiGeneratorError.generationFailed` after 3 LLM failures instead of returning a fallback with the real content hash. Stale fallbacks can no longer be cached. Regenerate buttons call `WikiArticleStore.invalidate()` to force fresh generation.
- **PC-202 [P1]**: Board wiki regenerate now works in WikiContainerView (looks up Board from article ID prefix). New `WikiEditSheet` lets users edit lead and section bodies. Both BoardWikiView and WikiContainerView expose Edit toolbar button.
- **PC-200 [P1]**: Replaced broken `PHPickerViewController` People picker with `PeopleCollectionPickerViewController` — a UIKit table showing `albumSyncedFaces` and regular user albums with thumbnails. iOS Photos on-device face groups have no public PHKit API; iCloud-synced face albums are the closest available substitute.

All 5 tasks → peer_review.

# Gem — 2026-05-02
- **PC-198 [P1]**: Implemented per-week hashtag graph in PrivateCore iOS. Built data retrieval in `SQLiteStore`, graph assembly in `GraphDataService`, and UI in `WeekGraphView`. Integrated into week wiki. Build green: build-green-2026-05-02-2.
- **PC-195 [P2]**: Created `docs/smoke_tests.md` with Sprint 5 test cases and updated photo injection requirements.