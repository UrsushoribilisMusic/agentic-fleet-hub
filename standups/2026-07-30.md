# Standup: 2026-07-30

## Activity Summary
- Agents called: 3 (clau, gem, tcr_scout)
- Sessions: 4
- Focus: Sovereign Mind TestFlight approval; Liquid LFM2 evaluation; PrivateCore/Lifelore repo diagnosis

## Notes

Fleet agents (gem, tcr_scout) idle. Direct work (clau):

### Sovereign Mind — event-ready
- **TestFlight build 1.2 (5) APPROVED** by Beta App Review → public link live: `https://testflight.apple.com/join/YT8Wr95K`. Confirmed Oleg (Apertus) knows of no other Apertus App Store app → **SM is likely the first Apertus iPhone app** (event headline for Thu/Fri).
- Ported the **background/Metal-revocation guard** from PC/SO into SM's engine (was missing) — closes a backgrounding-crash class in the live app. Committed + pushed.

### Liquid LFM2 — verified viable on iOS
- The `mlx-swift-lm` we already ship implements **LFM2 / LFM2-MoE / LFM2-VL** (VLMModelFactory registers them); mlx-community publishes on-device weights (350M–3B). So Liquid runs through the existing pipeline with **no new SDK**.
- MoE (e.g. LFM2-8B-A1B) saves compute not memory → wrong for iPhone; use dense small models.

### PrivateCore/Lifelore — LFM2-VL unified-model experiment
- Branch **`experiment/liquid-lfm2-unified`** (baseline tag `pre-liquid-lfm2-2026-07-30`): retire the two-model swap (Qwen2-VL + Ministral) for one **LFM2-VL-3B** doing image recognition AND wiki/text gen (~2 GB vs 4.5 GB, no hot-swaps). Added engine thermal gate + `increased-memory-limit` entitlement. Builds; needs on-device test.

### PrivateCore/Lifelore — repo contamination diagnosed + nav fix
- Root-caused missing screens: **~24 Silicon Oracle `SC-` commits leaked into the PC repo 2026-05-30→06-06** (SO "Sprint C", some by the Mistral Vibe agent), reskinning PC. `SC-003` swapped the tab bar (Home/Library/Search/Capture → Journal/Wiki/Boards), orphaning travel/contacts/boards screens (view files never deleted). Later PC work built on top → entangled.
- **Restored original nav + Wiki** on branch **`fix/restore-original-nav`** (builds). Full SO disentanglement is a deliberate post-presentation task.

### Cross-cutting
- Added memory + **fleet lesson** (`lessons` id kv8rsvwmj16klza, pending_review): "MLX on-device engines need six guards" (cache limit, clearCache-before-load, memory-pressure, thermal gate, background/Metal guard, memory entitlement). Silicon Oracle = reference; SM + PC brought to parity.

**Next:** SM on-device final walkthrough; Lifelore SO-untangling + clean tab design after the presentation; decide LFM2-VL adoption into main.
