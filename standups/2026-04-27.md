# Codi — 2026-04-27

## Activity Summary
- Agents called: 6 (clau, codi, gem, gemma, misty, tcr_scout)
- Sessions: 100
- Tasks completed: 278

## Session Summary

PrivateCore coordination:
- Reconciled open PrivateCore tasks from PocketBase back into [MISSION_CONTROL.md](/Users/miguelrodriguez/projects/private-core/PrivateCore/MISSION_CONTROL.md) and corrected owner drift for PC-083 through PC-086.
- Reassigned PC-083 and PC-086 to Codi; PC-084 and PC-085 to Gem.
- Moved remaining PrivateCore backlog items to `todo` so the team can pick them up.

New ticket creation:
- Created GitHub issues `#33` through `#42` in `UrsushoribilisMusic/privatecore-ios` for PC-087 through PC-096, then synced them into PrivateCore Mission Control and PocketBase.
- Created GitHub issue `#43` for PC-097 (BGTaskScheduler identifiers not advertised in built Info.plist), then synced it into PrivateCore Mission Control and PocketBase.
- Created GitHub issue `#225` in `UrsushoribilisMusic/agentic-fleet-hub` for the PocketBase GitHub identity collision fix (`github_repo + gh_issue_id`), then synced it into hub Mission Control and PocketBase.

Peer review outcomes:
- PC-071 approved after verification; implementation already present on `main`.
- PC-082 approved after static review of note-refresh behavior.
- PC-081 sent back to `todo`; branch restores add-to-board via a synthetic card path but does not correctly implement photo-backed add-to-people / add-hashtag behavior.

Findings / architecture notes:
- PocketBase `gh_issue_id` collisions are still present across repos; the numbers themselves are correct, but the shared schema is not unique without repo namespacing.
- BackgroundScheduler warnings on device are a real bug: the built app is not advertising `BGTaskSchedulerPermittedIdentifiers` correctly at runtime.

## Tasks Completed
- PrivateCore MC/PB reconciliation.
- PrivateCore reassignment cleanup.
- Creation and sync of PC-087..PC-097.
- Review decision updates for PC-071 / PC-081 / PC-082.
- Fleet follow-up ticket #225 for GitHub identity namespacing.

## Blockers
- None for coordination work.
- PrivateCore repo currently has unrelated in-progress local changes; no feature edits were made there beyond Mission Control/status sync.

## Next Steps
- Resume PrivateCore execution from PC-078, PC-083, PC-086, and the new `todo` queue.
- Implement #225 so PocketBase stops relying on bare `gh_issue_id` as a cross-project lookup key.

---

# Clau — 2026-04-27 (16:53 UTC)

## Session Summary
PC-031 peer-review fix. Codi rejected the prior submission because the Today calendar rows in `BriefingView.calendarSection` were plain `HStack` cards, not tappable destinations — the only AC gap remaining on "Deep-links from all items". Wrapped each row in `NavigationLink(destination: SearchView(initialQuery: event.title))` with `.buttonStyle(.plain)`, mirroring the existing `upcomingEventsSection` pattern. SearchView is the right destination — no `CalendarEventDetailView` exists in the codebase, and SearchView surfaces all locally-known references for the event title (same behavior Codi already approved for the Upcoming section).

## Tasks
- PC-031 (`ac8q6yh69hdyevp`) — fix landed in commit `b85bed5` on `origin/main`, output comment posted, status set to `peer_review`. Awaiting non-clau approval.

## Build
- `scripts/build-tag.sh` → BUILD SUCCEEDED in 13s, tagged `build-green-2026-04-27-14`.

## Blockers
- None.

## Notes
- PC-106 (PlaceService CLGeocoder rate limiting) has uncommitted work in the working tree on the PC-106 branch — left untouched, not in this commit.

---

# Codi — 2026-04-27 (22:44 CEST)

## Session Summary
Heartbeat-only session. Ran the full orient flow (`git pull`, `heartbeat_check.py`, `active_context.py`, active-project pulls, hub inbox) and reviewed the prefetched PocketBase snapshot at `/Users/miguelrodriguez/fleet/codi/pb_snapshot.json`.

## Tasks
- No `todo` tasks assigned to `codi` were present in the snapshot.
- No non-self `peer_review` tasks were present in the snapshot.

## Findings
- The snapshot currently contains only repeated self-assigned `peer_review` records for `PC-107` and `PC-108`, so Phase 2 and Phase 3 could not advance without violating the no-self-approval rule.
- `private-core` uses `origin/main`, not `origin/master`; `music-video-tool` is on `master` but its current `MISSION_CONTROL.md` is empty.

## Bookkeeping
- Wrote deferred PocketBase operations to `/Users/miguelrodriguez/fleet/codi/pb_pending_writes.json`: `working` heartbeat, one workflow lesson about the stale self-review-only snapshot state, then final `idle` heartbeat.

---

# Clau — 2026-04-27 (22:00 UTC)

## Session Summary
End-to-end device-test cycle with Miguel: triaged 15 device-found bugs, shipped 6 build iterations (v0.1.1 → v0.1.6), uncovered + filed structural fleet bugs (gemma + dispatcher), and reverted the Qwen2.5-VL 3B experiment after it tripped iOS jetsam.

## Key decisions / shipped
- **PC-099/100/101/102/103/104/105**: triaged from Miguel's device test, files split across gem/clau/codi/misty/gemma. PC-102/103/104 shipped same session.
- **PC-098 (Qwen2.5-VL 3B)** shipped on a task branch (codi) but never merged to main — discovered when device load showed old 2B still routing. Manually merged + bumped build, only to learn at runtime that Qwen2.5-VL 3B sits at ~2932 MB active and gets jetsam-killed on Miguel's device.
- **PC-107 [P1] reversed**: now retire Qwen2.5-VL 3B (not the 2B). Implemented in v0.1.6 (build 7) by deleting the 3B PCModelConfiguration and reducing ModelRouter vision candidates to `[qwen2-vl-2b-q4]`. Approved.
- **PC-108 [P1]** filed: 2-stage translate pipeline (Apple Vision OCR → Qwen2.5 1.5B Instruct Q4 text-only). Replaces VLM-OCR-then-translate (which "sometimes echoes the original text" — an artifact of small VLMs giving up on hard image-text). Big-sis recommendation re-validated; Qwen2.5 1.5B is also a candidate to retire SmolLM2 in the same change.
- **PC-097 reopened**: codi's previous "approved" was a gemma rubber-stamp lie. Verified by reading `Build/Products/Debug-iphoneos/PrivateCore.app/Info.plist` — neither `BGTaskSchedulerPermittedIdentifiers` nor `UIBackgroundModes` is in the compiled plist despite `INFOPLIST_KEY_BGTaskSchedulerPermittedIdentifiers` being set. Real fix recipe + verification step (`plutil -p` against compiled plist) added to scratchpad so the lie can't repeat.
- **Load-on-demand model lifecycle** (no ticket — small inline change): added `onDismiss` to ContentView's chat sheet that unloads MLXEngine, plus scenePhase=.background unload in PrivateCoreApp. Reduces idle RAM after chat sessions and on app suspend.
- **13 string-interpolation typos** fixed across CardDetailView + PlaceDetailView (`\\(...)` → `\(...)`) — visible bug was @mention pasting `[[\(person.displayName)]]` literally, plus 12 silent ones in tag chips, word counts, place dates, etc.
- **Dynamic version display** in Settings → About: replaced hardcoded `"v0.1.0"` with `Bundle.main.infoDictionary` reads. Versioning now bumps per build cycle (currently v0.1.6 build 7) so device install can be confirmed at a glance.

## Fleet meta
- Filed **#148 [P0]** (gemma Phase 3 doesn't actually execute work — `aichat -e` is non-interactive in launchd), **#149 [P0]** (gemma Phase 2 rubber-stamps every peer_review without verification — the source of the fleet-wide "approved without shipping" lies), **#150 [P1]** (dispatcher resets `assigned_agent` to original creator on completion). Concrete fix recipe in each. Recommended in #148 to drop gemma's code-task work entirely and restrict her to text-only summary/inbox tasks.
- Confirmed gemma actually runs (16:19 PC-088 attempt visible in `/Users/miguelrodriguez/fleet/logs/gemma.log`) but her output is the LLM's literal proposed shell command string, captured as "task output" without execution — no diff, no commit, status flipped to peer_review anyway.
- Confirmed reassignments via PB API revert to gem (#150 evidence: PC-098/099/100/101 all flipped back to gem within minutes).
- Multiple "approved" tickets verified as never-shipped: PC-083, PC-086, PC-087, PC-088, PC-089, PC-097, PC-098 (until manually merged today). Suggests a one-time audit pass after #148/#149 land — diff `status=='approved'` against `git log main` for matching `PC-N` strings.

## Cleanup
- Deduplicated **611 PocketBase records** (355 for PC-107, 256 for PC-108) — set status=approved with scratchpad pointer to canonical record so codi's peer_review queue is unblocked. Root cause is the github_sync identity collision (#225 already filed).

## Reassignment when gem ran out of tokens
- 8 of gem's active tickets redistributed: clau ← 098/099/100/101, codi ← 086/089/105, gemma ← 088. Most reverted within minutes (#150 in action). Gem reportedly came back online around 01:16 — rest of her queue can resume.

## Blockers (for the team overnight)
- **#148 + #149** are the structural blockers — until those land, "approved" remains untrustworthy. P0.
- **PC-097** (BGTask not in plist) is the lowest-hanging real fix but the false-approval pattern means it must be verified post-build with `plutil -p`, not just merged.
- **PC-108** (2-stage translate) is the highest-value user-visible improvement queued.
