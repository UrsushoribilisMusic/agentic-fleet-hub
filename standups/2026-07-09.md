# Clau — 2026-07-09 (Miguel session, Robot Ross)

## Activity Summary
- Sessions: 1 (Robot Ross, Windows laptop)
- Focus: switch Robot Ross demo brain from Apertus to Mistral ahead of the Mistral hackathon in Paris (~2026-07-19), fix several bugs found along the way

## Robot Ross — Mistral default + bugfixes (Clau / Miguel session)

**Model**: pulled `ministral-3:8b` via Ollama (official Mistral edge model, 6.0GB Q4_K_M) — newer/better fit for this CPU-only 16GB laptop than the stale `mistral:latest` (7B v0.3) already on disk; ruled out `mistral-small` (24B) as too heavy for live-demo latency.

**Brain switch (kept Apertus as fallback via `--brain`)**:
- `bob_ross.py` (narration), `skills/huenit/quickdraw_compose.py` (Quick-Draw scene composition), `skills/robot-ross/chat_ross.py` (conversation) all default to Mistral now.
- `skills/huenit/ai_sketch.py` (freeform SVG generation, previously Claude-Haiku-only) ported to Mistral too, with Haiku/Apertus as alternates. Added a defensive `<g fill="none" stroke="black">` wrapper since the local model doesn't reliably emit the pen-plotter styling Haiku did — confirmed the physical plotter parser (`huenit_svg.py`) only reads raw path/circle/ellipse geometry so this only affects on-screen preview, not the actual draw.

**Bugs found and fixed in the same pass**:
- Narration/scene-parse Ollama calls had a 30s HTTP timeout but real generation takes 55-90s on this hardware for any model — narration was silently falling back to canned filler text the whole time, even under the old Apertus default. Bumped to 120s.
- `bob_ross.py` blocked drawing start on that same 60-90s LLM call. Now generates narration in a background thread, speaks a fast generic filler intro immediately, starts drawing right away, and swaps commentary to the real request-specific phrases once generation lands.
- `control_center.py`'s sketch-preview subprocess timeout (60s) was shorter than `ai_sketch.py`'s own internal Ollama timeout (120s), so generation always got killed before finishing — bumped to 130s, and status now correctly surfaces real errors (was previously just spinning "Generating sketch…" forever on any failure, including once diagnosing that the account's Anthropic credit balance had run out).
- `skills/huenit/huenit_write.py`: multi-line `write` jobs crashed with `NameError: name 'dx' is not defined` the instant the arm tried to move to line 2 — silently looked like "only the first line gets written." Removed the stray tilt-X term from two Y-only moves.
- Control Center's "🔊 TTS Engine" dropdown defaulted to Kokoro and, separately, was never actually wired to `--engine` at all (changing it did nothing). Fixed default to Voxtral and wired it through; also consolidated the separate Showcase Mode "Voice" selector into the same shared control (both scripts accept an identical `--engine` flag).
- Added a dedicated "✏️ Write" button next to the Write textarea — previously the only way to trigger it was a global footer "Execute" button, easy to miss.

**UI trim for the hackathon**: removed the Image and Fetch tabs/panels from the Control Center (not needed for this demo).

**Repo hygiene**: `windows-local` branch was 3 months stale — 7 of 8 commits already duplicated into `master` under different hashes, 1 unique commit (`launch_drawing.bat`) ported over properly (with its stale `git checkout windows-local` self-reference removed). Branch deleted locally and on origin so it stops causing "which branch has the Windows work" confusion in future sessions.

**Commit**: `robot-ross` master `8956bee` (local only, not pushed to origin as of this entry — 21 files, includes the above plus two pre-existing local fixes picked up in the same pass: `calibration.json` tilt_slope_x field, `speak.py` corrected Voxtral voice slugs).
