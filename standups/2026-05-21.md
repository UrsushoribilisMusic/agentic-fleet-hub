# ReelTales + ATF — 2026-05-21

## Activity Summary
- Agents called: 0 (None)
- Sessions: 0
- Tasks completed: 0

## Summary
Full-stack day: wired cloud Apertus into ATF, migrated Classic Reel Tales story generation back to Claude, and redesigned the proof-of-work page with a proper editorial look.

---

## Changes Shipped

### ATF cloud LLM (agentic-fleet-hub + salesman-cloud-infra)
- Deployed 14-file wiki corpus to `/opt/salesman-api/atf/corpus/` (Subsystems/ + Topics/ subdirs)
- Added `loadAtfCorpus()`, `atfRankChunks()` (TF-IDF keyword ranking, topK=4), `callApertus()`, `atfQuery()`, `atfAnalyzeLog()` helpers to server.mjs
- Added `POST /atf/api/qa` and `POST /atf/api/analyze` endpoints
- Replaced mock JS rotation in `ATF/index.html` with real fetch calls to both endpoints
- `APERTUS_API_KEY` stored in `/etc/salesman-api.env` on server
- First key hit $1.00 budget cap → swapped to new key — updated both server env and music-video-tool `.env`

### Classic Reel Tales — migration to Claude (music-video-tool)
- `gen_story_modern.py`: removed `call_apertus()`, `translate_runway_prompts_to_english()`, `MODEL_CH`, `APERTUS_URL`, `import requests`
- All language routes (en, de-CH, fr-CH, it-CH) now use `claude-sonnet-4-6` directly
- Claude follows English-only instruction for Runway prompts natively — no post-translation step needed
- Documentation and landing page unchanged (still credits Apertus/Mistral for Reddit/marketing)

### Proof page redesign (salesman-cloud-infra)
- `rtProofPage()` fully rewritten — dark library background (`library-bg.jpg`, 353KB JPEG), warm overlay
- Fonts: Playfair Display (headings/hook) + EB Garamond (body) via Google Fonts
- No more scrollable monospace boxes: captions as flowing paragraphs with `· · ·` separators
- Hook displayed as large italic gold blockquote above the story
- Visual Vision (first_image_prompt + runway prompts) collapsed in `<details>` for curious creators
- In-progress and declined states updated with same background + typography

---

## Known issues / next session
- Infisical token still expired (all secrets fall back to .env — no breakage, but worth renewing)
- gen4_turbo not yet validated in a full production order — first real run still pending
- Apertus quota: Teddy to contact Swiss-AI team about budget model and higher limits
- Old YouTube video `CSBNflcFNUY` (bad French #1033 first upload) still pending manual deletion in YouTube Studio
