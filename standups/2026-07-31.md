# Standup: 2026-07-31

## Activity Summary
- Agents called: 3 (clau, gem, tcr_scout)
- Sessions: 3
- Focus: post-event follow-through — animated hero, repo share, YouTube revenue re-auth

## Notes

Fleet agents (gem, tcr_scout) idle. Direct work (clau):

### 🎉 Event was a success (Swiss AI / Apertus)
Many questions, several people came up afterward, **one possible new customer**. SM landed as (likely) the first Apertus App Store app.

### sm.flotilla.cc — animated hero demo (Bunny's idea)
Replaced the static phone mockup with a live-animated one that matches the real iOS app (light theme, **✈ in the status bar as the offline signal**, Chat/Wiki/Settings tab bar). **4-slide carousel**, auto-rotating, pauses off-screen, reduced-motion aware:
- **Chat** — types the question, streams the answer token-by-token with a cursor + citation
- **Wiki** — reveals article sections (1 / 2 / 2.1)
- **Admin console** — types a collection name → upload progress bar (72 chunks) → assigns a user
- **Settings** — downloads a model (progress → ✓) → selects the Field Engineer persona
Also fixed a latent bug (dead network-toggle script would have null-crashed and killed the page's scroll-reveals + contact form).

### Slovenia repo share
Invited **ales-jurancic** to `flotilla-corpus` (write). Was an overdue item.

### YouTube revenue / FinOps monetization
- **Re-auth DONE** — Miguel re-consented, granting `yt-analytics-monetary.readonly`. Ingestion ran: **44 videos OK, ~$0.051 (30d) across 11 monetized videos** now in the fleet SQLite.
- Committed the **401-handling fix** (`youtube_revenue.py`) — Google returns 401 (not always 403) for the missing scope, so the job now alerts gracefully instead of crashing.
- ⚠️ **FinOps tab still shows "awaiting data"** — the ingestion writes `revenue_30d` as a raw SQLite *column* but never registers it as a PocketBase *field*, so the API/dashboard can't see it. Also `channel_revenue_daily` returned 0 rows (RPM). **Follow-up: register the revenue fields on the songs collection schema (+ investigate channel-daily).** Minor: 3 videos errored on a startDate timestamp-parse bug.

**This afternoon:** Blindsight (security partner) call.
**Queue:** FinOps schema fix; Lifelore SO-untangling + LFM2-VL on-device test; customer slides (Big Sis, PPTX).
