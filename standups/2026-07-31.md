# Standup: 2026-07-31

## Activity Summary
- Agents called: 2 (gem, tcr_scout)
- Sessions: 2
- Tasks completed: 0

## Notes

Fleet agents (gem, tcr_scout) idle. Direct work (clau):

### 🎉 Event was a success (Swiss AI / Apertus)
Many questions, several people came up afterward, **one possible new customer**. SM landed as (likely) the first Apertus App Store app.

### sm.flotilla.cc — animated hero demo (Bunny's idea)
Static phone mockup → live-animated one matching the real iOS app (light theme, **✈ in the status bar = offline signal**, tab bar). **4-slide carousel**, auto-rotating, pauses off-screen, reduced-motion aware:
- **Chat** — types the question, streams the answer token-by-token + citation
- **Wiki** — reveals article sections (1 / 2 / 2.1)
- **Admin console** — types collection name → upload progress (72 chunks) → assigns user
- **Settings** — downloads a model (progress → ✓) → selects Field Engineer persona
Also fixed a latent bug (dead network-toggle script would have null-crashed and killed the page's reveals + contact form).

### Slovenia repo share
Invited **ales-jurancic** to `flotilla-corpus` (write).

### FinOps YouTube revenue — FIXED end-to-end
Miguel re-consented (granted `yt-analytics-monetary.readonly`). Then chased the monetization tab from "awaiting data" all the way to matching **YT Studio's $0.12**. Four root causes fixed + deployed across 3 repos:
1. **Schema** — `revenue_30d` was a raw SQLite column, never registered as a PocketBase *field* → registered (self-healing in ingestion now).
2. **Channel pull** — old single-day/per-country query returned 0 rows; replaced with a **30-day per-day `estimatedRevenue`+`views` pull** (all sources incl **Shorts**) = $0.118 / 511k views.
3. **fleet_push** — wasn't sending `channel_revenue_daily` at all → now it does (RPM/channel data reaches the dashboard).
4. **Dashboard** (`server.mjs`) — headline showed long-form only; now shows the **full channel total** (long-form kept as `longform_revenue_30d`). Deployed to robotsales, `salesman-api` restarted, backups taken.
- Also fixed the 401-vs-403 scope-probe crash + a startDate parse bug.
- **Result:** Revenue 30d = **$0.12** (matches Studio, incl Shorts), RPM computes, no more "awaiting data." Commits in music-video-tool, agentic-fleet-hub (fleet_push), salesman-cloud-infra.

### Also
- Blindsight (security partner) call this afternoon.

**Queue:** Lifelore SO-untangling + LFM2-VL on-device test; customer slides (Big Sis, PPTX); tiny startDate US-format edge on ~2 videos' lifetime revenue.
