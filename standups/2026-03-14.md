# Standup 2026-03-14

## Gem (Current Session)

### Done
- **Mac Mini Migration Verification**:
    - Updated home directory `~/.gemini/GEMINI.md` with the new Mac path for `MISSION_CONTROL.md`.
    - Created a dedicated `GEMINI.md` in the `agentic-fleet-hub` root, modeled after `CLAUDE.md` and following the shared mandate structure.
    - Fixed the `CRM-POC` project path in `MISSION_CONTROL.md` (renamed from `customer-mgmt` to `crm-poc` in the local workspace).
    - Verified `node` (v25.8.1) and `npm` (11.11.0) installations.
    - Verified git remotes (using HTTPS with osxkeychain credential helper).
    - Made `vault/agent-fetch.sh` executable for secret retrieval.
    - Acknowledged and marked all IAP inbox messages as read.

### Today
- **Migration Check**: ✅ Complete. Fleet infrastructure is functional on the Mac Mini.
- **Ticket #31 (Kanban UI)**: I have acknowledged Codi's handoff. Ready to proceed with any remaining UI polish if needed, though yesterday's standup marked it as closed. I will verify if there are any regressions after the migration.

### Notes
- **SSH Keys**: Noticed that `github-clau`, `github-codi`, and `github-gem` are missing from `~/.ssh/config` on this machine. However, since the current remotes are using HTTPS, this is not an immediate blocker for git operations.
- **Server**: Verified that the fleet-server is present and ready for `npm run dev`.

### Blockers
- None.

## Codi (Current Session)

### Done
- **Ticket #41**: Created `~/fleet/codi/CLAUDE.md` with the 6-phase heartbeat protocol aligned to the existing Gem and Clau fleet mandates.
- **Fleet workspace consistency**: Initialized `~/fleet/codi/PROGRESS.md` so the mandate's sign-off path exists immediately.
- **Mission Control update**: Marked `#41` closed in `MISSION_CONTROL.md` while preserving the in-flight `#32` cleanup already present in the file.
- **Ticket #44**: Diagnosed and repaired the live `api.robotross.art/fleet` auth/bootstrap path, deployed the corrected `salesman-api` server and dashboard assets, restored live lessons/inbox/standups/user management, and logged the auth loop issue in `AGENTS/LESSONS/ledger.json`.

### Today
- Verify whether any follow-up fleet automation expects a distinct Codex-named mandate file in addition to `~/fleet/codi/CLAUDE.md`.

### Blockers
- None.

---

## Clau (Session 2 — Miguel-invoked heartbeat)

### Done
- **#32** (Mission Control format hardening): Normalized all ticket numbers to `**#N**` format in both MISSION_CONTROL.md files. Moved all confirmed-closed tickets from OPEN table to CLOSED bullet list. Replaced invalid status values with v1 Kanban spec values (`planned`, `in_work`, `merged`). OPEN table is now parser-compliant.
- **#38** (launchd plists: PocketBase + dispatcher): Created `com.flotilla.pocketbase.plist` and `com.flotilla.dispatcher.plist` in `~/Library/LaunchAgents/`. Both KeepAlive, both loaded and running.
- **#39** (launchd heartbeat plists: Gem + Codi): Created `com.flotilla.heartbeat.gem.plist` (minute :00) and `com.flotilla.heartbeat.codi.plist` (minute :02). Full `/opt/homebrew/bin/node` paths throughout.

### Today
- All fleet always-on infrastructure (#34–#43) is now complete and live.
- Only open ticket remaining: **#41** (Codi mandate) — owned by Codi.

### Blockers
- None.

---

## Clau (Session 5 — Miguel-invoked heartbeat)

### Done
- Ran full 6-phase heartbeat protocol.
- Approved PocketBase tasks #35 and #37 (peer_review → approved); both already CLOSED in MISSION_CONTROL.
- Synced `~/fleet/MISSION_CONTROL.md` from git repo (was stale with #41 still OPEN).

### Today
- No open tickets. All fleet always-on infrastructure complete and live.

### Blockers
- None.
