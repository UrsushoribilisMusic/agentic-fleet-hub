# Clau Standup — 2026-06-23 (FX-022)

## Work Completed

**FX-022: Final QA checklist** — signed off, in peer_review.

All 6 guardrails checked against the full corpus:

| Check | Result |
|---|---|
| Eval leakage (18-entry ledger + FX-018 questions) | PASS — 0 in any SFT training file |
| Secrets / credentials / PII | FIXED — 1 Neon credential stripped |
| Duplicates across sets | PASS — _both files are wiki artifacts (subsets of _sft), not additional SFT inputs |
| Contested bucket isolated | PASS — 4 entries, zero overlap with SFT/wiki |
| Counts honest | PASS — corrections_sft 125→124 (credential drop), total SFT = 765 |
| Hard facts not in weights | PASS — wiki_corpus (269) has 0 ID overlap with SFT sets |

## Notes

- Credential found in `corrections_sft` record `fx010_c3887076c1a8`: Neon DB connection string (`npg_Vg6RGJ9kBMiF@ep-odd-bread-a2h0izic...`). Record had empty situation+judgment — no training value. Dropped.
- `redact_secrets.py` extended with `npg_` and `postgres://` patterns to prevent recurrence.
- FX-020 (Codi) must use only `*_sft.jsonl` files as SFT training inputs — NOT the `*_both.jsonl` files which are wiki cross-listing artifacts.
- Gate 2 near-dup cosine check remains pending (no embedding model at extraction time) — logged as FX-020 responsibility.
- Signed QA report: `fleet/corpus/FX-022-QA-REPORT.md`. Commit: `5b28c7d4`.

## Next

FX-022 in peer_review. Awaiting FX-020 (Codi: emit final datasets) and FX-021 (manifest) before package ships.
