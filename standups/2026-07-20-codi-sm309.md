# Codi — 2026-07-20 (SM-309)

## Activity Summary
- Task: SM-309: S9 — Wiki viewer (web)
- Status: moved to peer_review.

## Work Completed
- Implemented the first-class generated wiki viewer in `../sovereign-mind`.
- `/wiki/:collection` now renders a read-only wiki layout with section navigation, source document inventory, and per-section citations.
- Every generated wiki section carries source document plus page/section provenance.
- Published collections now build `generatedWiki` from attached documents, including source page and source section metadata.
- Deployed `server.js`, `wiki/index.js`, and updated smoke tests to `robotsales:/opt/sovereign-mind-backend/`; restarted `sovereign-mind-backend.service`.

## Verification
- Local `npm run test:all` in `../sovereign-mind` passed.
- Deployed `node scripts/test-access-control.js` passed on `robotsales` (31/31 assertions).
- Deployed `node scripts/test-admin-collections.js` passed on `robotsales`.
- Live `https://sm.flotilla.cc/wiki/onboarding` returns a backend 302 to `/auth/login?next=%2Fwiki%2Fonboarding`, confirming Caddy reaches Express for wiki routes.

## Blockers
- `../sovereign-mind` has no configured git remote, so commit `6f108441b7a007a09c5944177abb376162fb33cb` is local and the deployed server copy is the synced source of record for SM-309 until a remote is added.
