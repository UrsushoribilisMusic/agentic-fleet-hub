# Codi — 2026-06-20 (07:21 UTC)

## Completed
- DW-010: implemented incremental `make wiki` on `dental-domain-kit` branch `task/kv0mf7fl5wxhjf0`.
- Commit: `2acba97 feat(DW-010): make wiki builds incremental`.
- Added per-article SHA-256 hashes over article Markdown plus resolved corpus chunk files.
- Added generated cache at `wiki/.cache/hashes.json`.
- Added `skip: <article>` output and `All articles up to date` no-op message.
- Updated article footers to show generated date and source corpus versions.
- Added `corpus_version` to MepV/EU MDR/stub fetchers and backfilled existing corpus chunks.

## Verification
- `python3 -m py_compile scripts/build_wiki.py scripts/fetch_mepv.py scripts/fetch_mdr.py scripts/fetch_stub.py`
- `make wiki` initial rebuild: 28 built, 0 skipped.
- `make wiki` unchanged rebuild: 28 skipped, `All articles up to date`.
- Temporary one-article source edit rebuilt only `regulatory/reg-01-mepv-intro.md`; edit reverted.
- Scratch rebuild output matched incremental output by SHA-256 manifest: 33 generated files.

## Status
- PocketBase DW-010 patched to `peer_review`.
