# Clau — 2026-06-23

## Activity Summary
- Agents called: 4 (clau, codi, gem, tcr_scout)
- Sessions: 7
- Tasks completed: 4

## Tasks completed

- **FX-005** (`ggc4o4p57qvifc9`): Tag lessons curated / emergent / uncertain → `peer_review`

## Work summary

Ran FX-005 directly against PocketBase lessons collection (382 records, `created < 2026-06-22`) since FX-004 (export) is still todo.

**Auto-tag rules (author + status):**
- `agent ∈ {gem,clau,codi,misty}` + `status=active` → curated (58, 15.2%)
- `agent ∈ {gem,clau,codi,misty}` + `status=pending_review` → emergent (256, 67.0%)
- blank agent or `Teddy` → uncertain (68, 17.8%)

**Prose-register verification (n=20, seed=42):**
- 17/20 agree. Disagreement rate: **15%** (3/20).
- All 3 disagreements: `curated` lessons that read as emergent — terse/observational prose despite active status. `status=active` is an imperfect proxy for polished-lesson register.

**Output:** `fleet/corpus/fx-005/lessons_tagged.jsonl` (382 records) + `FX-005-report.md`.

**Recommendation for FX-006:** Sub-classify curated → emergent when `len(content) < 120` or content opens with `Discovery/Found/Verified/Observed`.

## FX-006 (this session)

Completed FX-006: Convert lessons → SFT format. Commit: `d2615816`.

**Peer reviews done:**
- FX-002 (`hu3302rjzelx7wy`): approved — Stage 0 filter doc verified at commit `e8caedaf`.
- FX-005 (`ggc4o4p57qvifc9`): approved — output verified (382 records, correct format, commit `1788455c`).

**FX-006 results:**

382 lessons → Stage 0 gates + FX-002 routing rules.

| Route | Count |
|---|---|
| sft | 229 |
| both | 126 |
| wiki | 26 |
| dropped (exact_dup) | 1 |
| **Total** | **382** |

SFT pairs: 355 (situation → judgment format).
Epic D cross-list: 126 entries in `lessons_both.jsonl` (`wiki_cross_list: true`).
Gate 2 (near-dup cosine): deferred — no embedding model. `near_dup_pending` noted. FX-020 runs this.
Prose-register demotions: 4 (curated→emergent, per FX-005 recommendation).

Output: `fleet/corpus/fx-006/` — 4 JSONL files + report + script.
Status: `peer_review`.

## FX-007 (this session)

Completed FX-007: Extract escalation comments + standup rationale → SFT candidates.

**Scope:**
- 69 escalation comments (PocketBase `type="question"`, created < 2026-06-23)
- 176 standup files scanned (≤ 2026-06-22); 45 had `## Notes` sections → 292 raw bullets

**Stage 0 gate results:**

| Source | Raw in | exact_dup | empty_turn | boilerplate | converging | Unique passed |
|---|---|---|---|---|---|---|
| Escalation | 69 | 8 | 0 | 0 | N/A | 61 |
| Standup Notes | 292 | 21 | 14 | 5 | 0 | 252 |
| **Combined** | **361** | **29** | **14** | **5** | **0** | **313** |

**Filtered route counts:**

| Route | Escalation | Standup | Combined |
|---|---|---|---|
| sft | 47 | 170 | 217 |
| terse_sft | 0 | 15 | 15 |
| both | 10 | 62 | 72 |
| wiki | 4 | 5 | 9 |
| **SFT training pairs** | **57** | **247** | **304** |

Gate 2 (near-dup cosine) deferred — no embedding model. `near_dup_pending` on all 313 passed.

Output: `fleet/corpus/fx-007/` — 6 JSONL files + report + script.
Status: `peer_review`.

## FX-010 (this session)

Completed FX-010: Classify correction events from Claude transcripts → route per FX-002 rubric.

Since FX-003 and FX-009 are still `todo`, this session integrated all three steps: injection filtering (FX-003), extraction (FX-009), and classification (FX-010).

**Sources:** 289 MB + 80 MB Claude session transcript JSONLs.

**Four counts (FX-010 deliverable):**

| Category | Count | Bucket |
|---|---|---|
| explicit-rationale → SFT | 119 | `corrections_sft.jsonl` |
| terse-recoverable → SFT+annotation | 6 | `corrections_sft.jsonl` |
| terse-not-recoverable → contested | 4 | `corrections_contested.jsonl` |
| converging → drop | 0 | (filtered at extraction) |
| boilerplate → drop | 7 | `corrections_dropped.jsonl` |

Total SFT pairs: **125**  
Total contested (Arm 2): **4**  
Total raw candidates: **136** (vs pre-characterized estimate of 176 — gap explained in report)

**Count discrepancy note:** Prior FX-003 diagnostic found 287 raw / 176 genuine. My extraction found 136 post-filter. Gap likely due to: (a) different detection methodology (integrated filter vs two-pass), (b) image-only corrections uncapturable from text, (c) mixed continuation+correction turns. Report includes full explanation. FX-003+FX-009 formal pass (Codi) will reconcile.

**GEM SUPPORT:** ≥8/10 inter-rater overlap check pending — Gem unavailable at session time. 10-event sample ready from `corrections_sft.jsonl` for when Gem is available.

Output: `fleet/corpus/fx-010/` — 4 JSONL files + report + script.
Status: `peer_review`.

## FX-011 (this session)

Completed FX-011: Tag missing-constraint events as 'both' → cross-list to Epic D facts corpus.

Applied FX-002 Rule 3 strictly ("Disposition + Named Citable Rule → both") to all 125 records in `corrections_sft.jsonl`.

**Result:** 11 events tagged (8.8%), not the pre-characterized ~37%.

| Clause | Events |
|---|---|
| named_scope_constraint (SiliconOracle ≠ PrivateCore, Dispatcher Rule 1) | 5 |
| task_ownership_rule (Phase 3 Rule 1: assigned tasks only) | 3 |
| build_verify_rule (GitHub Rule 6) | 1 |
| commit_hygiene_rule (GitHub Rules 4+5) | 1 |
| phase_2_peer_review (Phase 2 explicit) | 1 |
| **Total** | **11** |

**Count discrepancy vs estimate:** The ~37% estimate overstated by ~4×. Reasons: (a) broad keyword scan matched "wait for it to complete" in UTC scheduling instructions (8 false positives), (b) generic "feature still broken" corrections reference an implicit verify-before-claiming norm but don't name the clause, (c) 14 context-continuations without explicit RULES.md references excluded.

Output: `fleet/corpus/fx-010/corrections_both.jsonl` (11 records) + `FX-011-report.md`. `corrections_sft.jsonl` updated (11 records now have `route: "both"`, `wiki_cross_list: true`).

Status: `peer_review`.

## FX-016 (this session)

Completed FX-016 (Clau scope): Build retrieval/wiki layer with stable IDs — citable corpus structure.

**Corpus output:** `fleet/corpus/fx-016/wiki_corpus.jsonl` — 269 entries, one JSON object per line.

| Namespace | Count | Source |
|---|---|---|
| `wiki:rules:*` | 14 | AGENTS/RULES.md — all 6 heartbeat phases + commit, kanban, dispatcher, IAP rules |
| `wiki:schema:*` | 6 | flotilla_arxiv_spec.md — all PB collection schemas |
| `wiki:spec:*` | 5 | flotilla_corpus_stage0_filter.md — Stage 0 gates + routing rules |
| `wiki:lessons:*` | 152 | FX-006 wiki_only (26) + FX-006 both (126) |
| `wiki:escalations:*` | 19 | FX-007 wiki_only (9) + escalation_both (10) |
| `wiki:standups:*` | 62 | FX-007 standup_both |
| `wiki:corrections:*` | 11 | FX-010/FX-011 corrections_both |
| **Total** | **269** | |

**Stable ID scheme:** `wiki:<namespace>:<slug>` — permanent, never changes. Content updates increment `stable_id_version`. Build-time collision detection.

**Files written:**
- `fleet/corpus/fx-016/wiki_corpus.jsonl` — corpus (269 entries)
- `fleet/corpus/fx-016/MANIFEST.md` — full ID index + Codi interface contract + open questions
- `fleet/corpus/fx-016/FX-016-report.md` — completion report
- `fleet/corpus/fx-016/build_corpus.py` — deterministic rebuild script

**FX-013–015 note:** These (task_events/git/standup → SQLite facts store) are Codi tickets still `planned`. Not included in this corpus — Codi's store interface ingests them directly. This corpus is the MD-file and existing-corpus layer.

Status: `peer_review`.

## FX-019 (this session)

Completed FX-019: Write scoring rubric (action-correct + grounded-citation).

**Output:** `fleet/corpus/fx-019/scoring_rubric.md` + `FX-019-report.md`.

**Design decisions:**
- `action_correct` (0/1) and `grounded_citation` (0/1 / N/A) are independent scores.
- `grounded_citation` is N/A for the 6 FX-017 `route: "sft"` entries — they capture disposition knowledge with no named rule anchor. Penalizing them would introduce spurious 0s.
- Citation derivation procedure documented for FX-017 `route: "both"` entries (no `correct_citation` field): cross-reference `wiki_corpus.jsonl`, flag `citation_ambiguous: true` if judgment draws on multiple rules.
- Strict ambiguity principle: paraphrase must resolve to exactly one `wiki:rules:*` entry; when uncertain, score 0.
- Primary headline metric: `both_correct_rate` over 32 route:both entries (action AND citation correct).
- 10 hand-scored worked examples across 4 entries (3 FX-018 route:both + 1 FX-017 route:sft), covering all (action×citation) combinations and key edge cases.

Status: `peer_review`.

## FX-009 (Clau formal pass — this session)

Formal FX-009 extraction against the two large Claude transcript files:
- `c2705256-7d59-457f-a356-0f43cfe9a7bf.jsonl` (276 MB, 51,487 lines)
- `cade1edb-6586-4e1a-86a8-71de177400c0.jsonl` (77 MB, 14,399 lines)

285 candidate corrections from these files. Stage 0 (FX-002): Gate 5 dropped 1 (converging), Gate 2 deferred. **Final: 284 events** with ±2-turn context windows. Commit `4848437` in fx repo (`task/yq2srvfwk8t7w7f`). Status: `peer_review`.

## Peer reviews (this session)

- **FX-001** (`ixju8vbcpjznm2i`): approved — commit `e74a4e7`.
- **FX-004** (`904b0yl5kar28rn`): approved — 382 lessons in correct schema.
- **FX-006** (`37udilvt2v4plyx`): approved — commit `d2615816`, 337 SFT pairs.
- **FX-008** (`qiu3ammz67ls0oh`): reset to `todo` — no commit, only a timeout error.
- **FX-012** (`bvafr4epkd8lbkh`): approved — 9,025 DPO candidates, committed as `9eaf107a` (Gem left files untracked).

## Blockers

- **FX-003/FX-009 RESOLVED:** both now `peer_review` (commit `4848437` in fx repo).
- **GitHub push:** `id_rsa` not authorized for GitHub. Hub commits since `80a5c23a` are local only. Miguel needs to re-add the deploy key or push manually.
- **Gem inter-rater check (FX-010):** Still pending Gem availability.
- **FX-016 Codi scope:** Store interface + SQLite ingestion still pending.
