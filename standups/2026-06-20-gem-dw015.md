# Standup Update - 2026-06-20 - Gem

## Task: DW-015 [P1]: Eval set — held-out cases with expected outcomes and scoring rubric
**Status:** Completed (Staged for Review)

### What was done:
- **Evaluation Cases Authored:** Created a set of **20 evaluation cases** in `primitives/eval/cases.jsonl` under the `dental-domain-kit` repository.
- **Evaluation Dataset Properties:**
  - Placed in a separate `primitives/eval/` directory to ensure these held-out cases are never used during QLoRA fine-tuning.
  - Formatted each line to be compatible with both the raw governance structure (`id`, `behaviour`, `language`, `source_refs`, `instruction`, `response`) and the training prompt/completion schema (`id`, `prompt`, `completion`, `metadata`).
- **Balanced Case Types Included:**
  - **5 completeness_check cases:** Covers DE, FR, IT (including 2 cases with intentional missing lot numbers: `eval-gov-004`, `eval-gov-005`).
  - **5 refusal cases:** Covers DE, FR, IT (including 2 trap cases where fields look present but are invalid placeholders: `eval-gov-009` with PO Box/vague manufacturing location, `eval-gov-010` with placeholder patient ID `PAT-TEMP`).
  - **5 konformitaetserklaerung cases:** Standardised custom-made device declarations of conformity across DE (2), FR (2), and IT (1) referencing MepV Art. 10 / EU-MDR Annex XIII.
  - **5 tariff_structuring cases:** SSO LTZT dental technician tariff mapping (including 1 ambiguous job description `eval-gov-020` where the model must refuse to map and ask for material/restoration clarification).
- **Scoring Rubric (`primitives/eval/rubric.md`):**
  - Detailed explicit 0/1/2 grading rules for each targeted behavior.
  - Documented the expectations for trap and ambiguity handling to ensure strict compliance.
- **Documentation (`primitives/eval/README.md`):**
  - Detailed how the next sprint should execute the evaluation harness (inference setup, manual scoring, compliance gating).
  - Outlined target performance metrics (minimum overall score of 36/40 (90%) and strict 100% pass on compliance trap cases).
- **Verification:**
  - Authored a clean compilation script `primitives/generate_eval_cases.py` to prevent escape errors.
  - Validated that `cases.jsonl` has exactly 20 lines and parses successfully as JSONLines.
  - Staged all files in the current git branch.

### Blocking/Notes:
- No blockers. Staged and ready for review.
