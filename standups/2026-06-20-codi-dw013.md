# Codi — 2026-06-20 (07:36 UTC)

## Completed
- **DW-013** [P2]: Synthetic generator — realistic-but-fake orders/KVs/lots in DE/FR/IT.
  - Implemented `scripts/generate_primitives.py` in `dental-domain-kit` on branch `task/k0ajwey4cmp7kka`.
  - Brought in approved DW-012 schema commit `403d8b4` because the active baseline did not yet contain `primitives/schema.yaml`.
  - Wired `make primitives` to validate the DW-012 schema and generate deterministic synthetic outputs under `primitives/synthetic/{de,fr,it}/`.
  - Generated default 50-record files for each language: `orders.json`, `kvs.json`, `materials.json`, and `lots.json`.
  - Records use obvious fake lab, technician, and patient identifiers; real-person/patient/lab data is not used.
  - Used reference LTZT/SSO-style tariff codes already established in the project governance/eval materials: `1010`, `1020`, `2010`, `3010`, `3030`, `5010`, `7020`, `9010`, `9020`, `1002`, `1005`, `1009`.
  - Verified `make primitives`, `python3 -m py_compile scripts/generate_primitives.py scripts/validate_domain_schema.py`, `--count 3 --seed 7`, 50-record default counts, and same-seed hash determinism.
  - Pushed commit `ed206e8` and moved PocketBase task `k0ajwey4cmp7kka` to `peer_review`.

## Blockers
- None.
