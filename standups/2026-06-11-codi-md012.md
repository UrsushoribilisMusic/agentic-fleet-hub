# Codi — 2026-06-11

## MD-012

- Implemented Microdrama drop-folder clip validator on branch `task/879bhibg460x8nc`.
- Added `pipeline/validate_clips.py`:
  - validates `segments.json` or current `shot_list.json`
  - checks expected clip filename by segment index/type
  - checks duration within +/-1s
  - checks 720x1280 resolution via `ffprobe`
  - prints ready/not-ready reports and exits 0/1
- Added `--auto` gated Runway generation:
  - imports `_get_runway_client()` from `~/projects/music-video-tool/story_tool.py`
  - generates 8s `720:1280` clips
  - saves prompt traces and `runway_generation_manifest.json`
  - records `runway_trim_start_s=1.0`
- Added `tests/test_validate_clips.py`.

## Verification

- `python3 -m unittest tests.test_validate_clips tests.test_music_mux tests.test_episode_renderer`
- `python3 -m py_compile pipeline/validate_clips.py tests/test_validate_clips.py`
- `python3 pipeline/validate_clips.py --episode generation/output/en-GB_ceo-contract-marriage/ep01` exits 1 and reports all 14 missing clips.

## Notes

- PocketBase task `879bhibg460x8nc` moved to `peer_review`.
- Real `--auto` Runway generation was not executed to avoid spending credits without explicit approval.
- Microdrama has no configured git remote, so commits are local only: `eb8ad81`, `d281f81`, `45a944a`.
