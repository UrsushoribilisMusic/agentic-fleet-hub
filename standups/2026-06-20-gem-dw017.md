# Standup Update - 2026-06-20 - Gem

## Task: DW-017 [P1]: One-command demo — make serve + landing index framing the wiki
**Status:** Completed (Staged for Review)

### What was done:
- **Landing Page Framing (`wiki/index.html`):**
  - Updated the wiki generation script `scripts/build_wiki.py` to compile a framed landing page inside `wiki/index.html`.
  - Headline: "A cited, regenerable reference record of the dental-lab regulatory and tariff domain" (factual and confident).
  - Three bullets:
    1. Every article cites its source.
    2. `make wiki` rebuilds from sources in minutes.
    3. This documents what would go into the model.
  - Links: Added a call-to-action button linking to `show_rolf.html` (the compiled Meeting Guide) and another button linking to the first article (`conformity-conf-01-conformity-routes.html`).
- **Meeting Guide Processing (`SHOW_ROLF.md`):**
  - Updated `scripts/build_wiki.py` to automatically read `SHOW_ROLF.md` from the project root, compile it using the wiki's markdown compiler to `wiki/show_rolf.html` matching the clean white theme of the wiki, and copy the raw markdown file to `wiki/SHOW_ROLF.md`.
  - Rewrote relative URLs in `show_rolf.html` (e.g. converting `wiki/...` links) so all references are browser-clickable and work perfectly during local serving.
  - Enhanced `SHOW_ROLF.md` with explicit, clickable markdown links for all repository file/directory references (`sources.yaml`, `corpus/`, `articles/`, `primitives/`, and `primitives/eval/cases.jsonl`).
- **One-Command Serve Verification (`make serve`):**
  - Executed `make serve` which compiles the wiki automatically (via dependency on `wiki` target) and starts the threaded HTTP server on an available port starting at `8000` before opening the browser.
  - Confirmed the server starts, launches the default browser, and returns HTTP 200 for `/` (serving the newly framed landing page).
  - Verified Miguel can navigate from the landing page to `show_rolf.html` and to any of the 28 compiled articles without prep.

### Blocking/Notes:
- No blockers. Staged and ready for review.
