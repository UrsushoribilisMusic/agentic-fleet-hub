# Standup: 2026-04-26

## Activity Summary
- Agents called: 4 (clau, codi, misty, tcr_scout)
- Sessions: 26
- Tasks completed: 6

## Notes
No activity today - all agents idle

---

# Clau — 2026-04-26

## Device-test feedback session

User tested OOM-fixed VLM build on device, reported 7 issues. All addressed in commit `f5dfee8` on `private-core/main`. Build verified clean.

### Bugs fixed (4)

| # | Issue | Root cause | Fix |
|---|---|---|---|
| 1 | Scan saves image, not text | `CaptureView.saveToLibrary` always called `CardIngestionService.ingestImage` regardless of mode | Branched on `selectedMode == .scan` → run `OCRService.extractText(from: UIImage)` (new method) → save as `.text` Card |
| 2 | Photo search returns no results | `reindexAll()` skipped photos entirely; manually-added tags (e.g. `#dog`) never reached `search_index` | Added photos to `reindexAll()` (OCR text + GROUP_CONCAT of tag names); `addTag` now refreshes photo search index |
| 5 | Mood board shows photos only | `MoodBoardDetailView.loadPhotos()` only queried `fetchPhotosByTag`, ignored cards tagged with the board | Added parallel `fetchCardsByTag` + new `MoodBoardTextCell` view; `MoodBoardPhotoCell` now uses real `PHAsset` thumbnail (was placeholder rectangle) |
| 7 | Places shows no photo count | Not a code bug — `fetchPlaceSummaries()` reads counts from `place_links`, populated by `PlaceService.runClusteringCycle` (BackgroundScheduler) | Documented in commit msg. Photos without GPS won't link to places by design |

### Features added (3)

| # | Feature | Implementation |
|---|---|---|
| 3 | Trips — multi-note (day + global) | New `TripNoteSheet`. Toolbar `+` opens global; per-day `+` button in `DaySectionView` header opens day-scoped. Saves Card + `linkItemToTrip`. Day-scoped notes get a `yyyy-MM-dd` tag |
| 4 | Trips — photo long-press actions | `SmartPhotoGrid` now accepts `onLongPress: ((PhotoAssetRecord) -> Void)?` callback. `TripDetailView` shows `confirmationDialog` listing all mood boards / projects / persons; selection writes appropriate link |
| 6 | Library — long-press → board / project / person | Existing context menu had only "Add to Project"; added `AddToBoardMenu` + new `PersonLinkSheet` (uses `insertPersonLink`). Both `cardGrid` and `moodBoardGrid` updated |

### Files touched

- `Services/OCRService.swift` — added UIImage overload of `extractText`
- `Storage/SQLiteStore.swift` — `reindexAll()` includes photos; new `refreshPhotoSearchIndex(photoId:)` helper called from `addTag` for `sourceType == "photo"`
- `Views/CaptureView.swift` — scan mode branch saves text Card via OCR
- `Views/MoodBoardDetailView.swift` — `loadContent()` fetches photos + cards; new `MoodBoardTextCell`; real photo thumbnails
- `Views/LibraryView.swift` — `cardToLinkPerson` state, expanded contextMenus, new `PersonLinkSheet`
- `Views/TripDetailView.swift` — `SmartPhotoGrid.onLongPress` propagation, `DaySectionView.onAddNote`, toolbar/sheets/confirmationDialog wiring, new `TripNoteSheet`

### Build
`** BUILD SUCCEEDED **` for `generic/platform=iOS`. Only pre-existing warnings remain (deprecated `CLGeocoder`, Swift 6 actor isolation note in `BackgroundScheduler`, `try` on non-throwing in `PhotoIngestionService`).

### Commit
`f5dfee8` — device-test feedback: 4 bugs fixed + 3 features added

## Blockers
None. Awaiting user verification on device.

---

# Clau — 2026-04-26 (afternoon — Sprint 3 kickoff + fleet plumbing)

## Sprint 3 launch

Read user-provided handover doc `PrivateCore_Sprint3_Handover.docx` (16 tickets). Doc IDs PC-036..PC-046 collided with existing approved Sprint 1/2 tickets in PocketBase, so renumbered the whole sprint:

| Handover ID | New PB ID |
|---|---|
| PC-036 Unified Board refactor | **PC-063** (Codi) |
| PC-037-A view mode toggle | **PC-064** (Codi) |
| PC-037-B Board members universal | **PC-065** (Gem) |
| PC-037-C focus mode | **PC-066** (Misty) |
| PC-038 Trip notes inline | **PC-051** (Clau) ← shipped |
| PC-039 30-min event window | **PC-052** (Clau) ← shipped |
| PC-039-B Trip LLM summary | PC-053 (Misty) |
| PC-040 Country grouping | PC-054 (Gem) |
| PC-041 Place detail | PC-055 (Codi) |
| PC-041-B Place ↔ Board ↔ People | PC-056 (Gem) |
| PC-042 People Groups | PC-057 (Misty) |
| PC-043 Smart suggestions | PC-058 (Misty) |
| PC-044 Face clusters spike | PC-059 (Codi) |
| PC-045 Daily Note completion | PC-060 (Clau) |
| PC-046 Recent Captures | PC-061 (Clau) |
| (new) Library title bug | PC-062 (Codi — onboarding) |

All 16 created via PocketBase API (`/tmp/create_sprint3_tickets.py`). MISSION_CONTROL.md updated with assignments + critical path (PC-063 blocks 64/65/66/057; PC-059 needs spike before UI).

## Heartbeat gate bug — diagnosed + fixed

**Bug**: `fleet/heartbeat_check.py` Phase 0 watched only fleet files (MISSION_CONTROL.md, inbox.json, fleet_meta.json). PocketBase ticket creations (web UI / scripts) never tripped the gate. Codi was running every 10 min for 5 days but always exiting early with "No changes detected" because her PB snapshot was stale (last refreshed 2026-04-25 08:41 UTC).

**Fix**: added `__pb_state__` synthetic checksum to the watched set — sha256 over `(id, updated, status)` for tasks where `assigned_agent` matches any alias and `status` ∈ (todo, in_progress, peer_review). Includes a "PocketBase: N todo, M peer_review ticket(s) assigned to you" reason line so startup logs explain wakeups. Degrades gracefully if PB unreachable. Applied to BOTH copies (`/Users/miguelrodriguez/fleet/heartbeat_check.py` used by launchd + hub `fleet/heartbeat_check.py` source-controlled). Verified: first run exits 0 with PB-state changed reason, second run idle. Commit `cfbafbd` on hub.

**Side effect**: within 10 min of pushing the gate fix + MC update, Gem and Misty both woke up and started shipping Sprint 3 work (Codi only on next tick because of cron timing).

## Agents who woke up + worked

| Agent | Tickets shipped this afternoon |
|---|---|
| Gem | PC-054 (country grouping + flag emoji), PC-056 (Place ↔ Board ↔ People) — both `peer_review`. Also began Board scaffolding |
| Misty | PC-053 (Trip summary card UI + MLX wiring), PC-057 partial (NewPeopleGroupSheet refs), PC-058 partial (CoOccurrenceAnalyser) |
| Codi | PC-062 (Library card title bug — `b480267`), PC-059 spike result (PHAssetCollectionSubtype.smartAlbumPersons does NOT exist in iOS 26 SDK; Apple's People album uses private APIs — fallback to manual person creation per handover risk plan) |
| Clau | PC-060 (Daily Note device wiring) |

## Build broke 5 times today (and recovered each time)

Each agent shipped code with at least one compile error. Pattern: hallucinated method signatures (Misty's `ModelRouter.generateText`, `SQLiteStore.upsertTrip`), shell-escape garbling (Misty's `\/bin/bash.displayName`, `\\(count)`), incomplete refactors (Codi's `BoardDetailViewModel`/`BoardMemberCell` references with no definitions), color token typos (`pcSecondary` vs `pcSecondaryBackground`), missing `import Combine` for ObservableObject. Patched all forward in-session.

## Build-tag tooling shipped (PrivateCore-side)

To stop the "agents broke main since you last built" loop, added `private-core/scripts/build-tag.sh` (commit `96ac25c`):

- `scripts/build-tag.sh` — builds; if green, tags `build-green-YYYY-MM-DD-N`
- `scripts/build-tag.sh --status` — show last green tag + commits ahead
- `scripts/build-tag.sh --rollback` — `git reset --hard` to last green tag

First tag: `build-green-2026-04-26-1` at commit `9e2498f`. Pushed to origin so any device can `git checkout <tag>` for a guaranteed-installable snapshot.

## Other tooling

- `Settings → Data` now shows "Visual index (CLIP)" + "Visual index failures" rows so the user can diagnose CLIP photo search at a glance (commit `10b900a`).

## Cleanup needed

- One stray PB test record `ckx11lsyka6uhb6` titled "DELETE ME — test record from ticket creation" (PB anonymous-create rule allows POST but DELETE requires admin). User should delete via PB admin UI.

## Commits today (private-core/main)

```
96ac25c scripts: build-tag.sh — verify + tag green builds, instant rollback
9e2498f build fixes: PC-058/059 stubs + PeopleView completion
4130d4c docs: add pc-062 standup (Codi)
b480267 fix: sanitize camera capture titles (Codi PC-062)
80f009f docs: add pc-062 worklog (Codi)
0430c0f build fixes: insertBoardMember signature + ModelRouter call
6428c05 feat(PC-053): Trip summary — auto-generated overview card (Misty)
4c2e19b PC-056 Gem implementation + build fixes
e79aebb PC-054: Places — country grouping + city deduplication (Gem)
78771ac docs: Clau standup 2026-04-26 — PC-060 (Clau)
dd0589f feat(PC-060): wire Daily Note hero to device data (Clau)
10b900a settings: surface CLIP vector + failure counts to debug photo search
a5ac195 trip detail: inline notes + 30min event-match window (PC-051/052)
f5dfee8 device-test feedback: 4 bugs fixed + 3 features added
```

## Commits today (agentic-fleet-hub)

```
cfbafbd heartbeat: add PocketBase state dimension to gate
ddb2933 mission control: kick off Sprint 3 — 16 tickets PC-051..PC-066
1cd3b5c docs: Clau standup 2026-04-26 — device-test feedback (4 bugs + 3 features)
```

## Blockers
None. Build is green at `build-green-2026-04-26-1`. Codi confirmed alive — picked up PC-062 on her first cron tick after the gate fix.

