# Standup: 2026-04-26

## Activity Summary
- Agents called: 2 (clau, tcr_scout)
- Sessions: 3
- Tasks completed: 0 PocketBase tickets (work was direct device-test feedback)

## Notes
No activity today - all agents idle

---

# Clau — 2026-04-26

## Device-test feedback session

User tested OOM-fixed VLM build on device, reported 7 issues. All addressed in commit `f5dfee8` on `private-core/main`. Build verified clean.

### Bugs fixed (4)

| # | Issue | Root cause | Fix |
|---|---|---|---|
| 1 | Scan saves image, not text | `CaptureView.saveToLibrary` always called `CardIngestionService.ingestImage` regardless of mode | Branched on `selectedMode == .scan` → run `OCRService.extractText(from: UIImage)` (new method) → save as `.text` Card |
| 2 | Photo search returns no results | `reindexAll()` skipped photos entirely; manually-added tags (e.g. `#dog`) never reached `search_index` | Added photos to `reindexAll()` (OCR text + GROUP_CONCAT of tag names); `addTag` now refreshes photo search index |
| 5 | Mood board shows photos only | `MoodBoardDetailView.loadPhotos()` only queried `fetchPhotosByTag`, ignored cards tagged with the board | Added parallel `fetchCardsByTag` + new `MoodBoardTextCell` view; `MoodBoardPhotoCell` now uses real `PHAsset` thumbnail (was placeholder rectangle) |
| 7 | Places shows no photo count | Not a code bug — `fetchPlaceSummaries()` reads counts from `place_links`, populated by `PlaceService.runClusteringCycle` (BackgroundScheduler) | Documented in commit msg. Photos without GPS won't link to places by design |

### Features added (3)

| # | Feature | Implementation |
|---|---|---|
| 3 | Trips — multi-note (day + global) | New `TripNoteSheet`. Toolbar `+` opens global; per-day `+` button in `DaySectionView` header opens day-scoped. Saves Card + `linkItemToTrip`. Day-scoped notes get a `yyyy-MM-dd` tag |
| 4 | Trips — photo long-press actions | `SmartPhotoGrid` now accepts `onLongPress: ((PhotoAssetRecord) -> Void)?` callback. `TripDetailView` shows `confirmationDialog` listing all mood boards / projects / persons; selection writes appropriate link |
| 6 | Library — long-press → board / project / person | Existing context menu had only "Add to Project"; added `AddToBoardMenu` + new `PersonLinkSheet` (uses `insertPersonLink`). Both `cardGrid` and `moodBoardGrid` updated |

### Files touched

- `Services/OCRService.swift` — added UIImage overload of `extractText`
- `Storage/SQLiteStore.swift` — `reindexAll()` includes photos; new `refreshPhotoSearchIndex(photoId:)` helper called from `addTag` for `sourceType == "photo"`
- `Views/CaptureView.swift` — scan mode branch saves text Card via OCR
- `Views/MoodBoardDetailView.swift` — `loadContent()` fetches photos + cards; new `MoodBoardTextCell`; real photo thumbnails
- `Views/LibraryView.swift` — `cardToLinkPerson` state, expanded contextMenus, new `PersonLinkSheet`
- `Views/TripDetailView.swift` — `SmartPhotoGrid.onLongPress` propagation, `DaySectionView.onAddNote`, toolbar/sheets/confirmationDialog wiring, new `TripNoteSheet`

### Build
`** BUILD SUCCEEDED **` for `generic/platform=iOS`. Only pre-existing warnings remain (deprecated `CLGeocoder`, Swift 6 actor isolation note in `BackgroundScheduler`, `try` on non-throwing in `PhotoIngestionService`).

### Commit
`f5dfee8` — device-test feedback: 4 bugs fixed + 3 features added

## Blockers
None. Awaiting user verification on device.
