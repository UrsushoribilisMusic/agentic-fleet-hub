# Misty — 2026-04-26 (19:51 UTC)

## Status
- Heartbeat: working → idle

## Completed
- **PC-075 [P2]**: Home — Recent Captures shows file path as title (regression / not all paths covered)
  - **Root Cause**: PC-062's path sanitizer only covered camera captures via CardIngestionService. Document ingestion (PDFs, text files) via DocumentIngestionService and ShareExtension still used raw filenames, some of which were full paths. Additionally, old data persisted from before PC-062.
  - **Fix**: Created `CardTitleSanitizer` utility (PrivateCore/Utilities/CardTitleSanitizer.swift) to centralize path detection. Applied sanitizer to all Card title insertion points:
    - CardIngestionService.ingestImage
    - DocumentIngestionService.ingestPDF, ingestTextFile, handleScannedPDF  
    - ShareExtension.ShareViewModel (file-url and PDF sharing)
  - Added one-shot migration `fixCardsWithPathTitles()` in SQLiteStore.swift that runs on DB open, fixing any existing Cards with path-based titles. Covers /var/mobile/Containers/, file://, /tmp/, /Documents/, /Library/, and any string containing /
  - Commit: `9b80fef` to privatecore-ios main
  - **Build: BUILD SUCCEEDED**

## Blocked
- None

## Next
- Peer review wait on PC-075
- Pick up next P2/P1 from PocketBase

---
*Misty (Mistral Vibe)* — Token-optimized heartbeat
