# Standup: 2026-05-29

## Activity Summary
- Agents called: Claude Code (Miguel session)
- Sessions: 1 long session
- Tasks completed: SC-001 (fork + build confirmed)

## Done Today

### Fleet Hub — api.robotross.art/fleet/
- **Shift timeline fix**: `switchTimelineWindow()` now re-fetches heartbeats with the correct `?window=` param — 7d/30d views no longer show stale 24h data as all-grey
- **FinOps Classical Remix**: `SEEDED_TOTAL_SUBSCRIBERS=2209` fallback; live campaign rows enriched with seeded views/subscribers by name match; phase colors now distinct per phase; removed CR-004/CR-006 internal refs from UI; Shopify income read from `rtReadOrders()`
- **FinOps Classical Reels**: 2×2 panel grid formatting fixed; music stats + ReelTales stats links added
- **Stats page**: heatmap white background fixed; last-updated timestamp fallback ("Loaded HH:MM") when `generated_at` is null
- **Shopify webhook**: `total_price` + `currency` now persisted on order record going forward; order #1030 (Lukas Zeller, CHF 5.00) backfilled

### SiliconOracle — New project forked from PrivateCore
- **SC-001 COMPLETE**: `fork/siliconoracle-2026-05` tag pushed to `privatecore-ios`
- New private repo `UrsushoribilisMusic/silicon-oracle` created
- `PrivateCore.xcodeproj` → `SiliconOracle.xcodeproj`; all source folders renamed; bundle ID `com.bigbear.siliconoracle`; version reset to `0.1.0 (1)`; `CFBundleDisplayName = SiliconOracle`
- All PC worklogs / standups / architecture docs removed from SiliconOracle
- Fresh `MISSION_CONTROL.md`, `README.md`, `CLAUDE.md` bootstrapped from Sprint 0 brief
- **BUILD SUCCEEDED** on iPhone 16e simulator (warnings only, all pre-existing)
- SC-001–SC-012 Sprint 0 tickets created in PocketBase (`todo` status → Planned column)

## Lessons Learned
- PocketBase is the only kanban that exists — MISSION_CONTROL.md is a human mirror only
- New tickets must be created with `status: todo` — `approved` routes to the Done column

## Repos pushed
- `UrsushoribilisMusic/salesman-cloud-infra` → `bb3549e`
- `UrsushoribilisMusic/silicon-oracle` → `60ab14a`
- `UrsushoribilisMusic/privatecore-ios` → tag `fork/siliconoracle-2026-05`

## Next
- Codi: SC-002 (strip cut modules — unblocked)
- Misty: SC-010 (branding & oracle voice — unblocked)
- Miguel: confirm video style count for SC-006 (3 vs 4 styles), pricing for SC-009 (CHF 4.99?)
