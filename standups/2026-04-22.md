# Standup: 2026-04-22

## Activity Summary
- Agents called: 4 (clau, gem, misty, tcr_scout)
- Sessions: 8
- Tasks completed: 9

## Completed
- **kqjbq511 (PC-044)** — CLIP visual embeddings for photo content search (Clau)
  - `CLIPEmbeddingService` actor: MobileCLIP-S0 CoreML, 512-dim L2-normalised image + text embeddings
  - `clip_vectors` table in SQLiteStore with `upsertClipVector` / `fetchAllClipVectors`
  - `clip_embedding_status` column on `photos` table; migration guard for existing DBs
  - `VectorStore.searchClip` for CLIP-space cosine search
  - `SearchService` runs MiniLM text + CLIP visual + keyword in parallel; deduplicates by best score
  - `EmbeddingService.processRecord` skips MiniLM for text-less photos (CLIP handles them)
  - `PhotoIngestionService.processBatch` fires `CLIPEmbeddingService.startBackgroundProcessing()`
  - `BackgroundScheduler` has `clipVisualIndex` job type wired to CLIP service
  - `SettingsView`: "Pending visual index" stat + "Re-embed" button for manual re-index
  - Committed: `dc5e62d feat: PC-044 CLIPEmbeddingService + visual search integration`

## Notes
- MobileCLIP-S0 model files (MobileClipS0ImageEncoder.mlpackage + MobileClipS0TextEncoder.mlpackage) must be added manually to the Xcode target from https://huggingface.co/apple/coreml-mobileclip. Service is a no-op until models are bundled; existing text search continues to work.
- All 1300+ existing photos will need re-embedding once the model is in the bundle. Use Settings → Re-embed or it runs automatically on next app launch if CLIP is installed.
